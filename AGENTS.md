# Testhand — consignes agents

## Commandes
- Installer : `npm install` (workspaces `server` + `web` ; Node ≥ 20, Docker, npm).
- Base locale : `npm run db:up` (Postgres 17, port hôte **5433**) · `npm run db:schema` (rejoue db/schema.sql, idempotent) · migrations sur base existante, dans l'ordre : `docker compose exec -T db psql -U ygo -d ygo -v ON_ERROR_STOP=1 -f - < db/migrations/001-deck-configuration.sql` puis la même commande avec `db/migrations/002-profiles-and-conditions.sql`, puis `db/migrations/003-purge-legacy.sql` (étape 8 : purge du modèle historique ; simulation d'abord avec `-c "set testhand.purge_mode = 'simulate'"` avant `-f -`, puis application avec `-c "set testhand.purge_accept = '<empreinte du rapport>'"` ; base neuve = rien à purger, aucune acceptation), puis `db/migrations/004-side-plans.sql` (étape 10 : adversaires et plans de side ; additive, sans question, rejeu sans effet), puis `db/migrations/005-backoffice.sql` (back-office : `users.role`, tables `backoffice_*`, rôle PostgreSQL `testhand_backoffice` NOLOGIN à privilèges par colonne ; additive, rejeu sans effet et défensif — privilèges révoqués puis réaccordés), puis `db/migrations/006-annotation-defaults.sql` (annotations par défaut, docs/annotations-par-defaut.md : profil `reactive`, rôle `referent`, `card_flags.is_hopt` nullable (NULL = hérite) et `nonengine_choice`, `nonengine_groups.is_builtin` + groupe « Mulcharmy » (2) par compte, `card_references` et son journal en ajout seul `card_reference_log`, avis fermés par compte `user_notices` (partie D) ; exige 005 ; DDL idempotent hors journal, DONNÉES migrées une seule fois sous le second marqueur `006-annotation-defaults/c` (`false` → hérite, profils → choix, aperçus à NULL) ; rejeu sans effet).
- Back-office (docs/backoffice.md ; hors numérotation) : dossier `admin/` (.NET 10, Razor Pages, hors workspaces npm) — `dotnet build admin/Testhand.Admin.slnx -c Release` · `dotnet test admin/Testhand.Admin.slnx -c Release` (unitaires : scrypt RFC 7914 + hachages Node, TOTP RFC 6238, base32, AES-GCM, session ; intégration seulement si `BACKOFFICE_TEST_DATABASE_URL=postgres://ygo:ygo-disposable@127.0.0.1:55442/ygo` est posée, conteneur jetable `testhand-backoffice-db`, base vide exigée ; 150 unitaires + 16 d'intégration) · garde navigateur du back-office : `node admin/e2e/run.mjs` (pile jetable : conteneur `testhand-backoffice-e2e-db` sur 55443, site .NET Release sur 8795 avec le rôle restreint, Chrome installé ou `E2E_BROWSER` ; connexion, enrôlement puis vérification TOTP, chaque page, tri / recherche / pagination, thème retenu, déconnexion, à 1440 puis 360 px, puis bandeau PRODUCTION ; 49 vérifications, captures dans `admin/e2e/out/`, hors `npm test`) · rôle d'un compte (hiérarchie `admin > referent > user`) : `bash deploy/backoffice-role.sh grant <email> [--role referent|admin] [--apply] [--actor <qui>]` (défaut `admin`), `revoke <email>` (retour à `user`) et `list` (admins et référents) — simulation par défaut ; sur le VPS depuis `deploy/` avec `.env.prod` ; ailleurs `TESTHAND_DB_CONTAINER=<conteneur>`, jamais une base réelle sans demande explicite ; `referent` exige la migration 006 journalisée (refus nominatif sinon) — c'est le SEUL chemin d'attribution, jamais une route.
- Catalogue : `npm run migrate` (Supabase → local, upsert) · `npm run prune-cards` (simulation) · `npm run prune-cards -- --apply` · legacy : `npm run adopt -- <email> <mdp>`.
- Dev : `npm run dev` (API :8787 + Vite :5173) ou `npm run dev:server` / `npm run dev:web` ; `./start.ps1` = db + dev.
- Vérifier : `npm run typecheck` (serveur, web, puis `scripts/tsconfig.json` pour recompute-check.ts et annotations-report.ts) · `npm run build` · `npm test` (web Vitest puis server node:test).
- Annotations par défaut (docs/annotations-par-defaut.md) : détection pure dans `server/src/domain/cardDefaults.ts` (partagée avec le web, dans `__ENGINE_VERSION__`), gardée par `server/tests/cardDefaults.test.ts` sur des textes réels (`server/tests/fixtures/card-texts*.json`, `played-choices.json`) ; inventaire sur une base jetable : `node --import tsx scripts/annotations-report.ts --db postgres://…@127.0.0.1:<port ≠ 5433>/… [--out rapport.md] [--gap]` (aucune écriture, jamais la base de dev ; `--gap` = rapport d'écart par deck, choix seuls contre bibliothèque effective, base migrée à 006 exigée). Bibliothèque effective = choix du compte > référence commune > détection, fusion pure `web/src/lib/effectiveLibrary.ts` (dans `__ENGINE_VERSION__`), appliquée par le store (`deriveEffective`) et par `sourceFromDetail` (accueil, comparateur, fiche) : ne jamais construire un modèle moteur depuis `libraryState` seul. `card_flags.is_hopt` garde `default false` : toute insertion serveur écrit `is_hopt` EXPLICITEMENT (null = hérite). Interface (partie D) : un seul mode « Non-engine » (profil, étiquette facultative ; règle `lib/nonEngine.ts` : poser / adopter / retirer), plus de mode « Profil » ; les gardes e2e le pilotent par `[data-mode-trigger="nonengine"]`, `[data-ne-profile]`, `[data-ne-label]` et lisent l'origine par `data-origin-hopt` / `data-origin-nonengine` sur la tuile.
- Tests silencieux : `node scripts/test-quiet.mjs` (suite complète) · `node scripts/test-quiet.mjs web/src/engine/engine.test.ts` (un ou plusieurs fichiers). Après une modification ciblée, lance uniquement le fichier de test concerné ; la suite complète seulement avant de conclure une étape.
- Intégration PostgreSQL (base jetable sur 127.0.0.1:55433, procédure dans deploy/configuration-v2.md) : `TEST_DATABASE_URL=… npm run test:integration -w server` = suite `auth` (audit, lot 1 : app réelle par `buildApp`, garde privée par défaut, `/%61pi/…` → 401, email hostile, erreurs, identifiants serveur, bandeaux dus ; base vide exigée, réinitialisée à la fin ; 13 tests) puis suite `persistence` (base vide exigée, 17 tests) puis suite `purge` (étape 8 : migration 003, prune-stale-cards v2 — plans de side compris depuis l'étape 10 —, faux Supabase local, références communes depuis les annotations par défaut ; réinitialise la base jetable, 13 tests). Le `docker run` de la base jetable se lance depuis PowerShell, ou depuis Git Bash avec `MSYS_NO_PATHCONV=1` (vérifié en 7B, 11 tests verts).
- Gardes visuelles (étape 7, hors `npm test` et hors test-quiet.mjs) : `npm run e2e -w web` monte une pile jetable complète (conteneur PostgreSQL `testhand-e2e-db` sur 127.0.0.1:55434, schéma et migrations par stdin, cartes synthétiques, serveur 8790, Vite 5174, compte `e2e@example.test`), joue les scénarios `web/e2e/scenarios/` (`setup` = fixture Deck A / Deck B, `guards` = M1–M6 de 6B, `compare` = export réel comparé à l'écran à 1440, `mobile` = responsive du comparateur et du mur de mains à 360 / 390 / 768 / 1440 avec verdict par point et par largeur, `home` = aperçus de l'accueil recalculés sans cache faisant autorité (étape 9A), `conditions` = arbre ET/OU profond à 1440 / 360, `nonengine` = mode Non-engine unique (profil + étiquette facultative, poser / retirer, étiquette seule) vérifié par l'API à chaque clic (étape 9B, refondu en partie D des annotations par défaut), `extraside` = extra / side éditables (ajout en side, aucun recalcul, toast de zone, enregistrement, rechargement, 360 px ; étape 9C), `side` = plans de side (carte de side annotée sans recalcul, adversaire, sélection au clic gauche et droit, échange équilibré, plan incomplet puis prêt, chiffres persistés après enregistrement, lien profond `/decks/:id/side`, 360 px ; étape 10C), `sidesheet` = fiche imprimable (7 adversaires, cadres SORT / ENTRE, gros badge « ×n », noms masqués par défaut et case retenue, « — » avant calcul, « Tout calculer », chiffres persistés, impression en clair, PDF téléchargé de 3 pages au plus avec textes et illustrations) et comparateur sur un deck sidé (étape 10D), `defaults` = annotations par défaut (badges « auto », HOPT choisi puis retour au défaut, adopter / retirer un profil détecté, référence posée par un référent vue d'un second compte `e2e-other@example.test`, aperçus invalidés puis recalculés, page /references, bandeau une fois par compte, 360 px ; rôle et ancienneté du compte posés par `ctx.sql` dans le conteneur jetable ; cartes 90000030–31), fixture restaurée) sur le Chrome installé via `playwright-core`, puis démonte tout. Prérequis : Docker en marche (image postgres:17-alpine), Google Chrome (ou `E2E_BROWSER` = chemin d'un Chromium), ports 55434 / 8790 / 5174 libres — déplaçables par `E2E_DB_PORT` / `E2E_API_PORT` / `E2E_WEB_PORT`, que `rehearsal.sh` transmet aussi (utile quand un autre projet occupe 5174 : ne tue jamais son serveur, change de port). `E2E_WEB_PORT` seul ne suffit PAS : `E2E.base` est figée à l'import de `web/e2e/lib.mjs`, donc le navigateur continuerait de taper le 5174 (login → 404 sur le serveur du voisin) ; passe toujours `E2E_BASE=http://localhost:<port>` avec. Options : `-- --only guards,compare`, `-- --keep` (pile conservée pour itérer avec `-- --attach --only …`), `-- --down` (démontage d'une pile conservée). Captures et journaux dans `web/e2e/out/` (ignoré par git). Lance-la avant de conclure une étape qui touche l'interface ; jamais contre la base de dev.
- Sauvegarde, restauration, répétition (étape 8B, conteneurs jetables, jamais la base de dev) : `bash deploy/test-backup-restore.sh` (deux conteneurs 55438 / 55439, mutation M2 incluse) · `bash deploy/test-migration-sequence.sh` (55440, cas A–I de la séquence partagée de deploy/lib.sh : négatifs, base vide, volume neuf initialisé pendant l'attente ; jamais en même temps que l'e2e) · `bash deploy/rehearsal.sh --fixture` ou `bash deploy/rehearsal.sh <archive.sql.gz> --accept <empreinte>` (55436 + serveur 8790 + Vite 5174 + Chrome : restauration, séquence complète, recalcul contre l'ancien moteur e890078, e2e sur la pile migrée, retour arrière ; rapport dans `deploy/out/rehearsal-*/rapport.md`) · `node --import tsx scripts/recompute-check.ts --db <migrée> --old-db <pré-migration>` seul. Lance-les avant de conclure une étape qui touche deploy/, les migrations ou le schéma ; le dump réel reste hors dépôt (`../testhand-dumps/`).
- Prod : `bash deploy/deploy.sh` s'exécute sur le VPS uniquement (deploy/README.md §9 ; docs/deploy-runbook.md commande par commande — variante « déploiement courant » (étape 9C, C0–C6) pour tout déploiement depuis 8C : base non vide, 003 journalisée, aucune migration, aucune question attendue, archive pré-migration réelle de `deploy.sh` comme retour arrière des données, retour du code par `git checkout` + build + up ; variante « départ à vide » = 8C, exécutée le 8 sept. 2026 ; §3–§5 = base conservée, pour mémoire). Jamais depuis le poste.

## Architecture
Monorepo npm workspaces. `db/` : schéma normatif idempotent + migrations additives numérotées. `server/` : Fastify + pg — `src/domain/` contrat pur (configuration v2, archive JSON) partagé avec le web, `src/routes/` auth/cards/decks/library, `src/auth/` sessions et comptes, `scripts/` maintenance du catalogue. `web/` : Vite + React + Zustand + Tailwind — `src/engine/` moteur exact pur (énumération par composition, couplage maximum), `src/worker/` exécute le moteur hors UI, `src/store/` état de l'éditeur, `src/lib/` API, brouillons IndexedDB, modèle moteur, exports. `deploy/` : image Docker et compose prod derrière le Caddy du VPS. `docs/` : contrat métier, cas de référence, comptes rendus d'étape, spec, charte. Détail : docs/architecture.md ; décisions : docs/decisions-compressees.md.

## Règles
- Avant de toucher au moteur (`web/src/engine`), lis docs/regles-metier.md §4–5 et docs/cas-reference.md. Ne modifie jamais `engine/reference/oracle.ts` ni un test de référence pour faire passer le code.
- Avant de toucher au modèle de deck, aux routes decks/library ou au store, lis docs/etapes-2-3.md et deploy/configuration-v2.md.
- Avant de toucher au schéma ou aux migrations, lis server/AGENTS.md. Le modèle historique est purgé par la migration 003 (étape 8) ; `db/schema.sql` ne le recrée que tant que 003 n'est pas journalisée (bloc « Modèle historique », nécessaire à 001 sur base neuve).
- Avant de toucher au comparateur ou à l'export Excel, lis docs/spec-comparateur-decks.md et docs/regles-metier.md §5 « Comparateur ».
- Avant de toucher à l'interface, lis docs/design-system.md.
- Avant de toucher à deploy/, lis deploy/README.md et docs/deploy-runbook.md ; `deploy.sh` et `rehearsal.sh` partagent `deploy/lib.sh`, toute modification se rejoue avec `rehearsal.sh --fixture` et `test-migration-sequence.sh`. Aucune commande vers le VPS, Supabase ou une base réelle sans demande explicite.
- Toute décision prise en cours de tâche : ajoute-la à DECISIONS.md et une ligne à docs/decisions-compressees.md.
- Ne « corrige » jamais les valeurs de contrôle historiques (33,76 %, 3,54 %, 30,21 %) ni les IDs P/N/S/C/M des cas de référence.
- Commits : Conventional Commits en français (commitlint via husky). Style : aucun linter ni formatter configuré ; `npm run typecheck` est la seule garde, imite le fichier touché.

## Pièges
- `.env` unique à la racine, chargé par `server/src/env.ts` par chemin explicite (les scripts tournent avec cwd `server/`). En conteneur, tout vient de l'environnement. Ne l'affiche jamais (secrets).
- Le SQL monté dans `docker-entrypoint-initdb.d` ne s'exécute que sur un `pgdata/` vierge. Sur une base existante, rejoue schéma et migrations **par stdin** (`-f -`), jamais via le fichier monté (inode figé après `git pull`).
- Nouvelle migration = fichier `db/migrations/NNN-*.sql` + montage dans docker-compose.yml et deploy/docker-compose.prod.yml + rejeu dans la séquence de deploy/lib.sh (`run_migration_sequence`, partagée par deploy.sh et rehearsal.sh) + liste de `web/e2e/run.mjs` et cas I de `test-migration-sequence.sh` (`docker cp`). Depuis 8B, 001 et 002 ne se rejouent plus une fois 003 journalisée : 001 recréerait `deck_requirements` et 003 refuserait le rejeu (« objet historique réapparu ») ; les migrations additives POSTÉRIEURES à 003, elles, se rejouent dans les deux branches (004 depuis l'étape 10, 005 depuis le back-office, 006 depuis les annotations par défaut), et avant 003 — sinon leurs tables seraient vues « absentes avant » par le contrôle « 003 n'a touché que ses propres objets ». Ajouter un montage au compose de prod recrée le conteneur `db` au déploiement suivant (volume conservé, « Skipping initialization » ; l'app encore en service perd sa base quelques secondes avant son arrêt) — constaté au déploiement de l'étape 10.
- Migration 005 (back-office) : elle ajoute `users.role`, donc `users` change de FORME (md5 différent, effectif identique) la première fois qu'elle s'applique — `check_after_migration` le sait (`RESHAPED_BY_005`) et exige alors effectif identique et tous les rôles à `user` ; ne jamais retirer `users` de `KEPT_ACROSS_SEQUENCE` pour « faire passer ». `pg_dump` émet les `GRANT` vers `testhand_backoffice` : un cluster NEUF qui reçoit une archive prise après 005 doit pré-créer le rôle (`ensure_backoffice_role`, appelée par `restore_into_scratch_db` et `rehearsal.sh`), sinon `role "testhand_backoffice" does not exist` sous `ON_ERROR_STOP`. Le mot de passe du rôle n'est PAS dans la migration : `backoffice_db_login` (lib.sh) le pose depuis `BACKOFFICE_DB_PASSWORD` de `.env.prod`, rejoué par `deploy.sh` ; sans lui le rôle reste NOLOGIN. Le service `admin` écrit dans `backoffice_audit` à chaque requête : `hook_stop_app` (deploy.sh) et `restore.sh` l'arrêtent avec `app`, sinon la sauvegarde pré-migration vérifiée échoue (« la base a bougé »). `backoffice_audit` refuse `UPDATE` / `DELETE` / `TRUNCATE` à tout le monde (déclencheur) : ne jamais « nettoyer » le journal, même en test — recréer la base.
- Conteneurs jetables du back-office (`--label purpose=testhand-backoffice`, `--rm`, `--tmpfs`) : 55442 tests d'intégration .NET (`testhand-backoffice-db`), 55443 garde navigateur (`testhand-backoffice-e2e-db`, site sur 8795). `docker run` depuis PowerShell ou avec `MSYS_NO_PATHCONV=1`. Un lancement manuel du site (`dotnet run --project admin/src/Testhand.Admin -c Release` avec `BACKOFFICE_DATABASE_URL`, `BACKOFFICE_TOTP_KEY`, `BACKOFFICE_ENV=DEV`, `ASPNETCORE_URLS`) : jamais sur la base de dev avec le rôle `ygo` — créer le rôle restreint sur un conteneur jetable.
- Back-office, pièges de code : `page` est une clé de route réservée de Razor Pages, la pagination s'appelle `p` ; Razor HTML-encode les accents (`&#xE9;`) et traite `Mot@Expr` comme une adresse email (écrire `@("Mot" + Expr)`) ; le gabarit rend le formulaire de déconnexion AVANT le formulaire de la page, donc `button[type=submit]` et le jeton antiforgery à prendre sont ceux de `.form` (le dernier de la page) ; les tests .NET créent une instance d'app par test parce que la limite de débit compte aussi les connexions réussies ; Npgsql rend un `timestamptz` en `DateTime` UTC (`Db.ScalarAsync` convertit) ; un code TOTP ne sert qu'une fois : les tests avancent l'horloge simulée de 30 s avant chaque code.
- `prune-stale-cards` refuse de tourner tant que 003 n'est pas journalisée ; toute jsonpath `$.**` sur `deck_conditions.condition` doit être en mode `strict` (en lax, chaque élément de tableau est rendu deux fois).
- Patch de fichiers par script Node : `String.prototype.replace` interprète `$$` et `$1` dans la chaîne de remplacement (a corrompu les `do $$` de schema.sql une fois) ; passer une fonction (`s.replace(from, () => to)`).
- Conteneurs jetables de l'étape 8 (`--label purpose=testhand-step8`, `--rm`, `--tmpfs`) : 55433 intégration, 55434 e2e, 55436 répétition (`testhand-rehearsal-db`, avec `ygo_old` pour l'ancien moteur), 55438 / 55439 test sauvegarde-restauration, 55440 test de séquence, 55441 vérification d'une archive copiée (`restore.sh --check-only`), 55435 / 55437 inventaires ; jamais la base de dev 5433. Ne modifie jamais un script de deploy/ pendant qu'une répétition ou un test l'exécute : bash lit le fichier au fil de l'eau (un retour arrière est tombé pour cette raison en 8B, non reproduit ensuite).
- Les tests d'intégration refusent toute URL autre que `postgres://step23:step23-disposable@127.0.0.1:55433/step23` et exigent une base vide : conteneur jetable, jamais le DATABASE_URL de dev.
- `npm run build` affiche un avertissement Vite sur le chunk ExcelJS (~940 kB) : attendu, pas une régression.
- Sous PowerShell les docs utilisent `npm.cmd`. Si Vitest échoue au premier lancement (esbuild, Windows), relance `npm run test -w web -- --no-cache`.
- `web/src/lib/deckConfiguration.ts` importe `server/src/domain/deckConfiguration.ts` par chemin relatif : ce contrat doit rester sans dépendance Node/Fastify/pg.
- Le catalogue `cards` est un lookup sans FK ; `card_id` = passcode, parsé en Number par `server/src/db.ts`. `decks.summary` est un cache d'affichage (étape 9A), jamais une source de vérité : `__ENGINE_VERSION__` (vite.config.ts) = empreinte de `web/src/engine/*.ts`, `lib/engineModel.ts`, `lib/conditions.ts`, `lib/summary.ts` ; tout changement de ces fichiers invalide les aperçus (recalculés à l'accueil), rien à faire à la main ; un résumé n'est joint à l'enregistrement que s'il est frais (`lib/summary.ts`), jamais affiché s'il est d'une autre version.
- `migrate`, `prune-cards --apply`, `adopt` écrivent en base : sauvegarde avant, simulation d'abord.
- Anciennes écritures partielles → HTTP 410 ; `PUT /api/decks/:id` exige `expectedRevision` (409 sinon) ; deck d'autrui → 404, jamais 403.
- ExcelJS : import dynamique uniquement (chunk séparé chargé au premier export).
- PowerShell altère les accents d'un script passé à Node par un pipe (encodage par défaut) et ses apostrophes typographiques sont des délimiteurs : pour tout texte français, passe par un fichier explicitement UTF-8 ou un patch ; ne « corrige » jamais du code après une telle corruption.
- Postgres jetable des tests d'intégration : `docker run --rm --tmpfs … -p 127.0.0.1:55433:5432 postgres:17-alpine` (procédure dans deploy/configuration-v2.md) ; le port 55433 doit être libre, ce n'est jamais le 5433 de la base de dev.
- Sous PowerShell, `npm` est `npm.cmd` (`npm.cmd run …`) ; les exemples des docs suivent cette forme.
- En sandbox, esbuild (Vitest) peut être bloqué sur la lecture de `../../../..` et Docker sur son canal : relancer la commande hors sandbox, ne pas modifier la configuration pour contourner.
- Aucun lint ni formatter : `npm run typecheck` et les tests sont les seules gardes ; une suite verte ne dit rien du style, imite le fichier touché.
- Sous Git Bash (MSYS), `docker run --tmpfs /var/lib/postgresql/data` voit son chemin réécrit en chemin Windows (« mount path must be absolute ») : lance les commandes Docker des bases jetables depuis PowerShell, ou avec `MSYS_NO_PATHCONV=1`. `web/e2e/run.mjs` appelle Docker sans shell et n'est pas concerné.
- « · » dans une matrice ou un delta signifie zéro exact, jamais une faible valeur (étape 7, formateurs de `web/src/lib/fmt.ts`, mêmes règles que les formats Excel) ; l'arrondi n'a lieu qu'au rendu, la valeur fine reste dans l'infobulle.
- `deploy/lib.sh` exporte `MSYS_NO_PATHCONV=1` sous Git Bash (chemins Docker) : tout chemin passé à Node depuis un script de deploy/ passe par `host_path` (cygpath), sinon `/c/…` devient `C:\c\…`. Les dumps pg_dump ≥ 17.6 commencent par `\restrict` : restauration par psql seulement (conteneur), jamais par le pilote pg.
- `scripts/recompute-check.ts` matérialise l'ancien moteur e890078 par `git show` dans `deploy/out/reference-engine-e890078/` (ignoré par git) : ne jamais le copier dans le dépôt ni le modifier ; `scripts/` est un paquet ESM (`scripts/package.json`) pour l'await de premier niveau sous tsx. Les résumés `decks.summary` d'avant purge ne servent jamais de référence (cache de la dernière sauvegarde, périmé dès qu'une paire a bougé).
- Attente de la base (`db_ready`, deploy/lib.sh, étape 9B) : l'entrypoint de l'image PostgreSQL lance sur un volume neuf un serveur temporaire (socket) qui répond à `pg_isready` et à `select 1` puis s'arrête (« shutting down », incident 8C) ; `db_ready` exige `healthy` (si healthcheck), puis dans les journaux depuis `StartedAt` le dernier « ready to accept connections » après « init process complete » ou « Skipping initialization », puis `select 1`. Ne jamais revenir à `pg_isready` ni à un `select 1` seul ; cas I de test-migration-sequence.sh en est la preuve.
- Une base neuve ou déjà purgée n'a rien à purger : la séquence applique 003 sans acceptation ; un rejeu complet de la séquence (003 déjà journalisée) passe sans question et sans effet.
- Ne lance jamais `npm run e2e -w web` en même temps que `test-migration-sequence.sh` ou `rehearsal.sh` (Docker et CPU saturés : échec non reproduit de `setup` sous charge en 9B) ; toujours en série, et jamais en modifiant un script de deploy/ pendant qu'il tourne.
- Extra et side (étape 9C) : éditables (`addCard` / `setCopies` / `removeCard` prennent une zone, défaut `main`), enregistrés dans la même configuration, mais jamais dans le modèle moteur (`buildEngineModel` ne lit que `main`, contrat §2) : une mutation extra / side marque « non enregistré » sans recalcul (`zoneMutation`), le résultat reste frais et l'aperçu joint à l'enregistrement aussi ; convention 1–3 par carte ET par zone (la même carte peut être en main et en side) ; au-delà de 15 en extra ou side, avertissement seulement (`lib/zones.ts`). Gardes : `store/zones.test.ts`, équivalence 6A sur les trois zones, e2e `extraside`.
- Plans de side (étape 10B) : `web/src/lib/sidePlan.ts` est pur et HORS de `__ENGINE_VERSION__` ; `applyPlan` ne rend un main dérivé que pour un plan prêt ; les 3 indicateurs sont des critères du mode Requête (`indicatorCriteria`) ; l'empreinte d'un plan hache l'entrée COMPLÈTE du moteur du deck sidé avec la version, la position et les critères — ne jamais la réduire à la composition (une modification de bibliothèque doit périmer les chiffres). Garde : `lib/sidePlan.test.ts`.
- Plans de side (étape 10C) : les annotations du store passent par `annotationCalc` (recalcul seulement si l'entrée du moteur change — exact, pas par carte) et celles du compte par `libraryCalc` ; ne jamais revenir à `localCalc` inconditionnel sur une annotation. Les tuiles du side sont de vraies `CardTile zone="side"` (96 px ; le menu ⋯ retire du side) ; l'extra reste `ZoneCardTile`. `matchups` part dans la configuration et le brouillon, `planSummaries` jamais (cache) ; les chiffres d'un plan ne sont persistés que si rien n'est « non enregistré ». Onglet « Plans de side » (`SidePlanner`), lien profond `/decks/:id/side`. Gardes : `store/sidePlans.test.ts`, `lib/matchups.test.ts`, e2e `side`.
- Plans de side v2, partie A (docs/plans-de-side-v2.md) : `applyPlan(deck, plan, zoneOf)` — la zone de jeu d'une carte de plan vient de son type (`deckZoneOfType`, `server/src/domain/cardDefaults.ts`), jamais d'une colonne ; `zoneOfCatalog(cards)` la lit dans le catalogue chargé, donc tout appelant (store, `compareSideOf`, `sheetOf`) doit avoir chargé les objets `Card` des cartes de plan (`api.cardsByIds`), sinon zone inconnue = plan « à revoir ». `trySwap` (`lib/matchups.ts`) est la règle UNIQUE de l'échange, partagée par « Échanger » et l'aperçu (`lib/swapPreview.ts`) : ne jamais dupliquer une vérification d'un côté. `lib/sidePlanLegacy.test.ts` garde une copie figée de l'`applyPlan` de 471fadd : ne jamais l'adapter au nouveau code. `lib/studiedDeck.ts` est l'unique point de greffe de « Mains types ». Aucun de ces fichiers n'est dans `__ENGINE_VERSION__` ; `cardDefaults.ts`, lui, y est (corrigé en A : aperçus et chiffres de plan périmés une fois).
- Plans de side v2, partie B : l'orchestration (decks étudiés, aperçu, candidats) est dans `store/study.ts`, les règles dans lib/ — ne jamais mettre une règle dans l'orchestrateur ni un calcul dans lib/. Trois clients de calcul (principal, aperçu, candidats), un par usage : ne jamais poster l'aperçu sur le client principal (il attendrait derrière un `computeAll`), ni appeler `cancelAll` (annulation par tâche). Une réponse n'est adoptée que si sa clé d'entrée est encore attendue. Les caches sont au niveau du module : `resetStudyEngine()` dans `beforeEach` de tout test du store qui étudie un adversaire, sinon un test « voit » les entrées calculées par le précédent. Le mode `second` du worker existe (passe second seule). Le contexte d'étude vit dans `?contre=&position=` (`lib/studyUrl.ts`, `replaceState`) et nulle part ailleurs. Gardes : `store/study.test.ts`, `lib/studyUrl.test.ts`.
- Fiche et comparateur sidé (étape 10D) : `/decks/:id/side/fiche` (`SideSheet`, page autonome, son propre client de calcul) et `/compare/:a/:b` avec un segment `deck~adversaire~position` (`lib/comparison.ts` : `parseCompareTarget`, `compareSideOf`) lisent le deck ENREGISTRÉ — boutons de l'onglet désactivés tant que quelque chose n'est pas enregistré. L'impression en clair passe par `@media print` (index.css) et les classes `print:` : l'écran reste sombre. « Télécharger le PDF » (`lib/sideSheetPdf.ts`, jsPDF en import dynamique comme ExcelJS, texte ramené au Latin-1 par `pdfText`) lit les images par canvas : elles doivent être de même origine — relais authentifié `GET /api/cards/:id/image`, le CDN n'envoyant pas de CORS — ou `data:` (cartes synthétiques de l'e2e) ; ne jamais y passer l'adresse du CDN. Gardes : `lib/sideSheet.test.ts`, `lib/sideSheetPdf.test.ts`, `server/tests/cardImage.test.ts`, e2e `sidesheet`.
- Commandes VPS (sur demande explicite seulement) par `ssh goldfish bash -s <<'EOF'` : `docker compose exec -T` et `backup.sh` lisent l'entrée standard et avalent la suite du heredoc — `< /dev/null` sur chacun ; `deploy.sh` se lance détaché (`nohup bash -c 'bash deploy.sh < /dev/null; echo "EXIT $?"' > ~/deploy-<horodatage>.log 2>&1 &`), toute question étant alors refusée. L'app ne journalise pas les requêtes : une écriture d'utilisateur pendant un déploiement se date par les archives (sauvegarde de C2 contre archive pré-migration).

Plan courant : docs/PLAN.md — lis-le au début de chaque session, ainsi que le dernier compte rendu d'étape.
- Refonte visuelle (audit 01, docs/refonte-visuelle.md) : aucune couleur ni taille en dur dans un composant. Texte = `text-fg-1..4` (`fg-4` = méta seulement, jamais sur `ink-850`) et accents `text-pos/warn/info/neg` ; fonds / bordures = `ink-950..500` (valeurs dans les variables de `index.css`, `ink-400..100` n'existent plus) ; tailles = échelle FERMÉE `text-meta` 11 · `body` 13 · `value` 14 · `head` 16 · `title` 18 · `hero` 20 · `glyph` 24, `text-cell` 10 réservé aux cellules compactes du comparateur sous 640 px. `text-xs`, `text-sm`, `text-[10px]` ne produisent rien (ou contournent le plancher) : ne jamais les réintroduire. Formules de carte de chaleur dans `lib/colors.ts` seulement. Éditeur : une seule `<nav>` d'onglets rendue (haut dès 640 px, bas en dessous) ; les gardes e2e désignent un onglet par `nav button[title="Nom complet"]` et le bouton d'enregistrement par `button[data-save]` (son libellé devient « enregistré 10:04 » au repos).
