# Plan de la mission de fiabilisation — 9 étapes

Source du plan : docs/cas-reference.md, « Acceptation par étape du plan révisé ».
Cible : docs/regles-metier.md (contrat métier). Comptes rendus détaillés : docs/etapes-*.md.
Chaque session lit ce fichier au début et met à jour le statut et le compte rendu en fin de tâche.

## Étapes

| # | Étape | Résultat vérifiable attendu | Statut |
| --- | --- | --- | --- |
| 1 | Règles et exemples | Contrat traçable ; fractions de contrôle ; oracle distinct ; vérifications existantes maintenues | ✅ Terminée le 7 sept. 2026 — contrat métier, cas de référence P/N/S/C/M, oracle indépendant (`web/src/engine/reference/`), 26 tests ; aucun code applicatif modifié. |
| 2 | Calculs confirmés défectueux | Tests de régression reliés au moteur et à l'affichage ; masses, arrondis et conditions corrects | ✅ Terminée le 7 sept. 2026 — delta reconstruit après remplacement, colonne 5+ agrégée, passe impossible explicitement indisponible, note /10 à arrondi exact, binomiale bigint ; 6 tests (`corrections.test.ts`). |
| 3 | Modèle et persistance | Paires manuelles propres au deck, non-engine commun au compte, enregistrement cohérent, import/export et duplication testés | ✅ Terminée le 7 sept. 2026 — configuration v2 (paires par deck, conditions, disponibilités, révision, transaction unique), migration additive `001-deck-configuration.sql`, JSON v2, duplication ; tests web/serveur/PostgreSQL, migration jouée en base jetable seulement. |
| 4 | Recalcul | État périmé visible, actions possibles, invalidation immédiate, annulation et absence de réponse ancienne adoptée | ✅ Terminée le 7 sept. 2026 — client de calcul pur avec propriété des tâches et annulation (`worker/computeClient.ts`), versionnement `modelVersion`/`resultVersion`, invalidation dès la mutation, état périmé atténué avec son contexte, erreur + relance, ouvertures numérotées pour `loadDeck`/`saveDeck` ; 16 tests avec faux worker et horloge simulée (`recompute.test.ts`, `computeClient.test.ts`). |
| 5 | Chronologie et conditions | Comparaison exacte à l'oracle sur 5 + pioche ; mêmes règles dans tous les consommateurs ; ET/OU testés | ✅ Terminée le 7 sept. 2026 — partie A (moteur : contexte unique, sixième identifiée, profils, plafonds partagés, ET/OU ; oracle étendu + oracle Monte Carlo) et partie B (migration additive `002-profiles-and-conditions.sql`, profils/plafonds au compte, conditions ET/OU par deck, invalidation des résumés à toute modification globale, réglage unique premier/second, sixième carte distinguée, « départs théoriques », Excel et comparateur relibellés) ; 150 tests web, 6 serveur, 10 PostgreSQL. |
| 6 | Configuration, création et mobile | Même deck métier depuis création/import ; erreurs explicites ; actions et sélections utilisables aux largeurs retenues | ✅ Terminée le 7 sept. 2026 (tags `etape-6a-ok`, `etape-6-ok`) — partie A : parseur YDK et collage qui rapportent sans réduire, aperçu d'import avec décision explicite, constructeur strict, messages serveur rendus tels quels, ids catalogue stricts, équivalence création / YDK / JSON v2 ; partie B : validation au navigateur (étape 4, contexte, sixième carte, mode Profil, menu ⋯, ET/OU, avertissement sans profil, aperçu d'import) avec captures sur infrastructure jetable, defaut serveur du plafond partagé corrigé (CHECK avant ON CONFLICT), Q3 tranchée (delta de tuile atténué), mobile 360 / 390 / 768 corrigé (retours à la ligne, cibles 32 / 24 px) et largeurs inscrites au contrat §6 ; 164 tests web, 7 serveur, 11 PostgreSQL. |
| 7 | Comparateur et exports | Matrices côte à côte sur mobile, deltas non arrondis, données identiques entre analyse/Excel/comparateur | ✅ Terminée le 8 sept. 2026 (tags `etape-7a-ok`, `etape-7-ok`) — partie A : identité des données prouvée (`dataIdentity.test.ts` : matrice du panneau = comparateur = cellules Excel en égalité stricte, agrégats et formules Excel recalculées, carte sans profil et plafond partagé), « · » réservé au zéro exact et couleur par signe exact (D1, D7 ; formateurs partagés), gardes visuelles versionnées dans `web/e2e/` (`npm run e2e -w web`, pile jetable, M1–M6, fixture Deck A / Deck B, comparateur avec export réel) ; partie B : scénario `mobile` (verdict par point et par largeur à 360 / 390 / 768 / 1440, 24 captures, export réel), Q3 tranchée (A et B côte à côte sous 640 px en grille de deux colonnes à cellules compactes de 9 px et 20 px minimum par colonne, Δ en dessous en pleine largeur, lisibilité prouvée sur capture à 360, aucun repli à défilement), Q4 appliquée (32 px sur Exporter Excel, Comparer, ✕, ⇄ Inverser), en-tête du comparateur en `flex-wrap`, ligne de main du mur qui passe à la ligne au lieu d'écraser les cartes, gardes correspondantes (7 mutations détectées), contrat §5 / §6 et charte §6.3 mis à jour ; 177 tests web, 7 serveur, 11 PostgreSQL. |
| 8 | Déploiement | Scripts dans /deploy, inventaire et simulation, purge exacte annoncée, contrôles après migration et restauration testée | ✅ Terminée le 8 sept. 2026 (tags `etape-8a-ok`, `etape-8b-ok`, `etape-8c-ready`, `etape-8-ok`) — A : inventaire prouvé du modèle historique, migration `003-purge-legacy.sql` (refus nominatifs, rapport ligne par ligne, simulation annulée volontairement, acceptation par empreinte, purge exacte, contrôles, rejeu défensif), `schema.sql` à bloc historique conditionnel (D1), `prune-stale-cards` v2, suite `purge.integration.ts`, rapport de simulation sur le dump réel (185 paires, 15 exclusions, 15 prérequis convertis, 4 pertinences ; empreinte `e0efff5c…`). B : `deploy/lib.sh` (séquence de migration partagée par `deploy.sh` et `rehearsal.sh`, codes 0 / 1 / 2 / 3, marqueurs « SIMULATION TERMINÉE » / « PURGE APPLIQUÉE » exigés avec le code 0), `backup.sh` étendu sans changer le cron (`.sha256`, restauration dans `ygo_verify`, `.fingerprint`, `keep/` hors rétention), `restore.sh` réversible par bascule de bases (refus sans somme ou empreinte, sauvegarde de sécurité, `ygo_previous`, `--check-only`), `deploy.sh` (sauvegarde obligatoire, inventaire avant 001, simulation, « OUI » / `--expect` / `--accept`, contrôles après migration, app démarrée sans 003 si refus, arrêtée seulement sur contrôle KO), `rehearsal.sh` (répétition complète sur copie jetable : restauration, séquence, recalcul contre l'ancien moteur e890078 sur la base pré-migration, e2e sur la pile migrée, retour arrière), `scripts/recompute-check.ts`, `test-backup-restore.sh`, `test-migration-sequence.sh`, `docs/deploy-runbook.md` ; 001 et 002 ne se rejouent plus une fois 003 journalisée (rejeu prouvé). Répétition sur le dump réel conforme (empreinte `e0efff5c…` reproduite, 16 / 16 decks identiques à l'ancien moteur avec paires réinjectées, e2e conformes, retour arrière prouvé). Préparation de 8C le 8 sept. 2026 (tag `etape-8c-ready`) : décision « départ à vide » (rien n'est conservé, archive souvenir hors VPS par principe), variante V1–V7 du runbook, catalogue prouvé (`migrate-cards.js` depuis le conteneur `app`, 14 529 cartes en 13–16 s, `/api/health` = source), contrôle « base vide au départ » ajouté à `lib.sh` (cas G / H). C : exécutée sur le VPS le 8 sept. 2026 par l'utilisateur (variante « départ à vide », code `9d281cf`) — archive souvenir vérifiée et copiée hors VPS, volume recréé, 001–003 jouées à vide par `initdb`, `deploy.sh` sans question (code 0 au second lancement), catalogue chargé par `migrate-cards.js`, compte créé, contrôles V7 passés, app en service. Incident consigné (docs/etape-8.md « Compte rendu 8C », runbook V4) : au premier `deploy.sh` sur le volume neuf, « Attente de la DB » (`pg_isready` par le socket) a accepté le serveur temporaire d'initialisation de PostgreSQL ; l'empreinte initiale a échoué pendant son extinction (« the database system is shutting down »), code 1, rien d'appliqué ; second lancement conforme. `deploy.sh` non corrigé : report en étape 9. |
| 9 | Finitions interface | Réponses de 7B (docs/etape-7.md, « Réponses ») appliquées : (1) mur de mains sous 640 px compacté, récapitulatif « S 3 · U 1 » (+ note) à droite des cartes, cartes jamais déformées ; (2) « ↻ Nouvelles mains » promu en action primaire de 32 px, même règle qu'Enregistrer (6B) ; (3) densité de la grille d'annotation à 1440 px : tuile réduite si une disposition conserve trois cibles de 32 px, sinon 96 px et clore ; arbre ET/OU plus profond que « ET de clauses, OU de feuilles » validé au navigateur, corrigé seulement si cassé ; (4) Δ en cellules de 10 px sous 640 px (pleine largeur) ; plus le report hors étape : aperçus de l'accueil recalculés avec une version fiable, sans cache faisant autorité. Gardes `web/e2e/` correspondantes. | ✅ Terminée le 9 sept. 2026 (tags `etape-9a-ok`, `etape-9b-ok`, `etape-9-ok`) — plan validé le 9 septembre 2026 (docs/etape-9.md), découpage 9A / 9B / 9C. **9A** (tag `etape-9a-ok`) : aperçus de l'accueil recalculés sans cache faisant autorité (version du moteur `__ENGINE_VERSION__` = hash des sources du calcul ; résumé joint à l'enregistrement seulement s'il est frais ; recalcul à la demande à l'accueil, passe premier seule, persisté par `PUT /decks/:id/summary` avec révision, 409 ignoré ; un résumé absent ou d'une autre version n'est jamais affiché) ; finitions 7B : mur compact sous 640 px (récapitulatif « S / U » + note à droite des cartes, ligne ≤ 80 px, huit mains par écran), « ↻ Nouvelles mains » à 32 px, Δ en cellules de 10 px à toute largeur, densité de la grille close à 96 px (84 px mesuré : delta coupé à 1440), arbre ET/OU profond validé au navigateur (seule correction : commandes de l'éditeur à 24 px) ; scénarios e2e `home` et `conditions`, gardes `mobile` / `guards` étendues ; 188 tests web, 8 serveur, 12 + 10 PostgreSQL ; 7 mutations détectées. **9B** (tag `etape-9b-ok`) : attente de la DB par `db_ready` (lib.sh, utilisée par `deploy.sh` : `healthy`, puis dernier « ready » après le marqueur de l'entrypoint « init process complete » / « Skipping initialization », puis `select 1` ; cas I de test-migration-sequence.sh avec témoin du serveur temporaire, 61 gardes A–I) ; vue du panneau transitoire (retirée des params émis, acceptée en lecture, « changer de vue ne salit pas ») ; mode Non-engine + profil combiné (déroulant étiquette + profil radio, « profil inchangé » par défaut, règle unique `nonEngineEffect`, deux requêtes au plus dans la file, badge « poser / retirer » sur la tuile et bandeau, mode Profil conservé, scénario e2e `nonengine` vérifié par l'API, fixture intacte) ; 200 tests web, 8 serveur, 12 + 10 PostgreSQL ; 4 mutations détectées ; questions ouvertes Q9 / Q10 (confirmées ensuite). **9C** (tag `etape-9-ok`) : extra et side éditables (`addCard` / `setCopies` / `removeCard` avec zone, `main` par défaut ; mutation extra / side = « non enregistré » + brouillon sans recalcul, jamais dans le modèle moteur ; convention 1–3 par carte et par zone ; repère 15 = avertissement seulement ; un bloc par zone toujours rendu, tuiles de 80 px à stepper 32 px, « + Ajouter » ouvrant le dialogue sur la zone, toast et annulation sensibles à la zone ; `store/zones.test.ts`, équivalence 6A étendue aux trois zones sans modifier les tests existants, scénario e2e `extraside` Z1–Z5), runbook « déploiement courant » C0–C6 (base non vide, 003 journalisée, aucune question, archive pré-migration réelle = retour des données, `git checkout` = retour du code ; = cas « F rejeu » de test-migration-sequence.sh), clôture (contrat §6, charte §6.3, architecture, AGENTS.md, DECISIONS.md, decisions compressées) ; 206 tests web, 8 serveur, 12 + 10 PostgreSQL, 8 scénarios e2e, 61 gardes A–I, répétition conforme ; 6 mutations détectées ; questions ouvertes Q14–Q18. |

## Mission suivante — plans de side (étape 10)

Plan validé le 10 septembre 2026 : [docs/etape-10.md](etape-10.md) (intention, décisions D1–D18 du
cadrage, modèle, règles R1–R9, découpage, preuves). La mission de fiabilisation ci-dessus est close ;
cette étape ajoute une fonctionnalité, elle ne la rouvre pas.

| # | Partie | Résultat vérifiable attendu | Statut |
| --- | --- | --- | --- |
| 10A | Modèle, contrat, persistance | `Configuration.matchups` validé en structure seulement, migration `004` additive rejouée dans la séquence, lecture / écriture dans la transaction unique, ids stables, archive et duplication | ✅ Terminée le 10 sept. 2026 — contrat (adversaires, plans, `MATCHUPS_MAX`), `004-side-plans.sql` (3 tables, clé naturelle adversaire + position), `deckRepository` en upsert, `GET /decks/:id` et duplication, état du store et aller-retour JSON ; 3 tests de contrat, 1 test d'intégration persistance, 1 test de purge (`prune-stale-cards` étendu aux plans), 3 tests web ; 68 gardes A–I (cas « base à 003 sans 004 » ajouté), répétition conforme ; 7 mutations détectées ; `__ENGINE_VERSION__` inchangé. |
| 10B | Deck sidé calculable | `applyPlan` pur, 3 indicateurs bâtis sur `queryProbability`, cache versionné et sa route, identité stricte avec le mode Requête | ✅ Terminée le 11 sept. 2026 — `lib/sidePlan.ts` (application du plan et ses trois statuts, deck sidé = main édité à la main, indicateurs = requêtes du mode Requête, R7, empreinte FNV-1a de l'entrée complète du moteur), route `PUT …/plans/:position/summary` et `plan_summaries` ; 17 tests web, 1 de contrat, 1 d'intégration ; 7 mutations détectées ; `__ENGINE_VERSION__` inchangé. |
| 10C | Annotation du side + vue Side | Bloc side annotable sans recalcul hors main, vue `/decks/:id/side` (copies dépliées, échange équilibré, états incomplet / à revoir), chiffres et écart, scénario e2e `side` | ✅ Terminée le 11 sept. 2026 — onglet « Plans de side » + lien profond `/decks/:id/side`, deck de base à copies marquées, recalcul seulement si l'entrée du moteur change (`annotationCalc`, `libraryCalc`), bloc side en vraies tuiles, `lib/matchups.ts`, sources neutralisées, chiffres et écart ; 17 tests web, scénario e2e `side` ; 5 mutations détectées ; contrat §8, charte §6.3. |
| 10D | Comparateur étendu + fiche | `/compare` sur un deck sidé, fiche imprimable (swaps, 3 chiffres, note libre), « tout calculer », scénario e2e `side-fiche` | ✅ Terminée le 11 sept. 2026 — fiche `/decks/:id/side/fiche` (deck enregistré, « — » avant calcul, « Tout calculer » dédoublonné, impression en clair, **une page A4 pour sept adversaires** prouvée par PDF), comparateur sur `deck~adversaire~position` ; 6 tests web, scénario e2e `sidesheet` ; 4 mutations détectées. **Étape 10 close** (docs/etape-10.md §14). Retouche du 11 sept., après déploiement : cadres SORT / ENTRE, gros badge « ×n », PDF téléchargé, **3 adversaires par page A4** (§15). |

## Hors numérotation — back-office (socle en lecture seule)

Plan validé le 16 septembre 2026 : [docs/backoffice.md](backoffice.md) (décisions 1 à 11, choix
techniques T1 à T16, modèle et privilèges, découpage A / B, vérifications, questions ouvertes).
Hors de la numérotation des étapes : c'est un outil d'exploitation, pas une fonction du produit.

| Partie | Résultat vérifiable attendu | Statut |
| --- | --- | --- |
| A — socle | Migration `005-backoffice.sql` (`users.role`, tables `backoffice_*`, rôle PostgreSQL `testhand_backoffice` à privilèges par colonne, journal en ajout seul par déclencheur) ; `deploy/backoffice-role.sh` (simulation par défaut, `--apply` journalisé) ; séquence de `lib.sh` (005 dans les deux branches, arrêt d'`admin`, mot de passe du rôle, pré-création du rôle à la restauration) et `check-migration.sql` ; cas 005 de `test-migration-sequence.sh` ; solution .NET avec scrypt (RFC 7914 + hachages Node), TOTP (RFC 6238), chiffrement du secret, politique de session, tests unitaires. Tag `backoffice-a-ok`. | ✅ Terminée le 16 sept. 2026 — 005 (rôle `testhand_backoffice` NOLOGIN à privilèges par colonne, déclencheur d'ajout seul, rejeu défensif), `backoffice-role.sh` + `.sql` (GUC, simulation, `--apply` journalisé `cli`), `lib.sh` (005 dans les deux branches avant 003, `ensure_backoffice_role`, `backoffice_db_login`, `users` reformée contrôlée), `check-migration.sql` (garanties du rôle vérifiées à chaque déploiement), cas « F sans 005 » et J de `test-migration-sequence.sh` (86 gardes A–J), scrypt / TOTP / base32 / AES-GCM / session en C# (150 tests unitaires, RFC 7914 vecteurs 1–3 + 8 hachages Node, RFC 6238 + 4226) ; typecheck, build, 254 web + 14 serveur, 12 + 14 + 11 PostgreSQL, 10 scénarios e2e, répétition conforme ; mutations détectées (docs/backoffice.md §10). |
| B — site | Razor Pages (connexion, TOTP, tableau de bord, comptes, fiche, journal) branchées avec le rôle restreint ; tests d'intégration sur 55442 (refus sans session / cookie joueur / compte `user` / sans TOTP / après 8 h / après retrait du rôle ; une ligne de journal par consultation ; aucun contenu de deck ; compatibilité scrypt) ; charte « corpo, factuel » clair / sombre (`docs/design-backoffice.md`) ; garde navigateur 360 / 1440 ; image, service Compose, runbook « première mise en ligne du back-office ». Tag `backoffice-ok`. | ✅ Terminée le 16 sept. 2026 — Razor Pages (`/login`, `/totp`, `/`, `/comptes`, `/comptes/:id`, `/journal`, `/logout`, `/health`) avec le rôle restreint, journal explicite par page, en-têtes CSP sans inline, thème clair / sombre retenu, bandeau PRODUCTION / DEV ; 16 tests d'intégration sur 55442 (refus sans session / cookie joueur / user / sans TOTP / 8 h / rôle retiré, une ligne par consultation, aucun contenu de deck ni privilège, scrypt Node, débit, ligne de commande) ; garde navigateur 49 / 49 à 1440 et 360 ; `Dockerfile.admin`, service `admin`, runbook B0–B7, README §10, charte `docs/design-backoffice.md` ; mutations détectées (docs/backoffice.md §10). |

## Hors numérotation — annotations par défaut (HOPT et profils pré-remplis)

Plan validé le 16 septembre 2026 : [docs/annotations-par-defaut.md](annotations-par-defaut.md)
(inventaire prouvé sur le catalogue et l'archive du 8 septembre, décisions D1–D15 et révisions,
règles R1–R7, réponses Q1–Q9, découpage A–D). Trois couches : détection textuelle → référence posée
par un référent → choix du compte.

| Partie | Résultat vérifiable attendu | Statut |
| --- | --- | --- |
| A — détection | `server/src/domain/cardDefaults.ts` (pur, partagé, dans `__ENGINE_VERSION__`), fixtures de textes réels, tests d'accord avec les choix existants, `scripts/annotations-report.ts` | ✅ Terminée le 16 sept. 2026 — HOPT `byName` seul (4 623 cartes du catalogue), profils par 4 gabarits (réactive / flexible / précoce + plafond Mulcharmy / board breaker), 97 des 99 HOPT du compte expert retrouvés sur 126 cartes jouées, 7 tests, 6 mutations détectées (docs/annotations-par-defaut.md §12). |
| B — contrat et moteur | Profil `reactive` (contrat §3, moteur, oracle, libellés), porte profil / étiquette levée (cas Q1 réécrit sur décision, garde serveur), migration 006 ouverte, tag `annotations-b-ok` | ✅ Terminée le 16 sept. 2026 — profil réactif (moteur, oracle N01, pont B02), porte levée (Q1 réécrit sur décision, gardes serveur / store / grille), 006 (contrainte CHECK, branchée partout, 99 gardes A–J, répétition conforme), 291 web + 21 serveur, 12 + 14 + 11 PostgreSQL, 10 scénarios e2e, 5 mutations détectées (docs/annotations-par-defaut.md §14). |
| C — modèle et persistance | Migration 006 (rôle `referent`, `is_hopt` nullable, `nonengine_choice`, groupe fourni de base, `card_references` + journal, résumés à NULL), routes, `effectiveLibrary.ts`, archive JSON, `prune-stale-cards`, `backoffice-role.sh`, séquence de déploiement, rapport d'écart, tag `annotations-c-ok` | ✅ Terminée le 17 sept. 2026 — 006 étendue (données une fois sous `/c`), routes de choix par aspect et de références (référent, 404, journal, invalidation tous comptes), bibliothèque effective dans le store et hors éditeur, archive et purge du catalogue, rapport d'écart (`--gap` : P(≥ 1) et brick inchangés sur les 16 decks du 8 sept., E[U] seul change) ; 301 web + 22 serveur, 12 + 17 + 13 PostgreSQL, 130 gardes A–J, répétition conforme, 10 scénarios e2e, 10 mutations détectées, relecture indépendante (docs/annotations-par-defaut.md §15). |
| D — interface | Pastilles d'origine, retour au défaut, bandeau, mode Non-engine par profil, formulaire et page de référence, e2e `defaults`, docs, tag `annotations-d-ok` | ✅ Terminée le 17 sept. 2026 — réponses préalables (adopter puis retirer, bandeau par compte, /references aux référents) ; `user_notices` dans 006 et route de fermeture ; mode Non-engine unique (poser / adopter / retirer) ; badges d'origine, retour au défaut, formulaire et page de référence, bandeau ; e2e `defaults` ; 309 web + 22 serveur, 13 + 17 + 13 PostgreSQL, 133 gardes A–J, 11 scénarios e2e, répétition conforme, 9 mutations détectées, relecture indépendante (docs/annotations-par-defaut.md §16). |

## Hors numérotation — refonte des plans de side (deck sidé au centre de l'éditeur)

Plan validé le 22 septembre 2026 : [docs/plans-de-side-v2.md](plans-de-side-v2.md) (inventaire prouvé, mesures sur la fixture et
l'archive du 8 septembre, décisions D1–D17, règles S1–S10, réponses Q1–Q13, révisions §11). Prompt :
[prompt-refonte-plans-de-side.md](prompt-refonte-plans-de-side.md).

| Partie | Résultat vérifiable attendu | Statut |
| --- | --- | --- |
| A — règles pures | Extra dans les plans (zone par type, équilibre par zone), `isMainDeckType` corrigé, légalité du deck, deck étudié et aperçu en pur, non-régression des plans existants prouvée ; tag `side-v2-a-ok` | ✅ Terminée le 22 sept. 2026 — `deckZoneOfType`, `applyPlan(deck, plan, zoneOf)` par zone avec extra dérivé hors moteur, `trySwap` unique (échange et aperçu), `undoSwap` / `clearPlan`, `deckLegality`, `studiedDeck.ts`, `swapPreview.ts`, garde D15 contre la copie figée de 471fadd (fixtures + 500 plans), contrat §2 / §8 (S1–S10) ; 355 web + 22 serveur, `--gap` avant / après identique sur les 16 decks du 8 sept., 7 mutations détectées + 1 équivalente, relecture indépendante (docs/plans-de-side-v2.md §12). |
| B — store et calcul | Contexte d'étude (adversaire, position) dans l'URL, résultats par deck étudié et cache, aperçu, écarts de candidats sur un second client ; tag `side-v2-b-ok` | ✅ Terminée le 22 sept. 2026 — mode `second` du worker, `store/study.ts` (decks étudiés par entrée du moteur, deux temps passes → complet derrière le deck de base, cache, aperçu et candidats sur leurs propres clients, persistance des chiffres de plan), état et actions du store (`setStudy`, sélection, `commitSelection`, `undoLastSwap`, `clearOpenPlan`, `runCandidates`), contexte dans `?contre=&position=` (`lib/studyUrl.ts`, `replaceState`) ; 373 web + 22 serveur, 11 scénarios e2e, 9 mutations détectées + 1 équivalente, relecture indépendante (défaut bloquant C1 corrigé : annotation de side → resynchronisation) (docs/plans-de-side-v2.md §13). |
| C — éditeur en contexte | Barre de contexte, panneau, matrice, requête, mur et tuiles sur le deck étudié et nommés ; e2e `study` ; tag `side-v2-c-ok` | ✅ Terminée le 22 sept. 2026 — `StudyBar` (une seule bascule, sélecteur natif, raison d'un plan sans chiffre), `columnOf` / `studiedSource`, panneau / matrice / requête / mur / tuiles sur le deck étudié et nommés (S1, S2, S3), badges « sort / entre » et bandeau de l'onglet Annoter ; e2e `study` (C1–C7, 4 largeurs), `guards` et `mobile` déplacées sur la barre ; 376 web + 22 serveur, 5 mutations détectées, relecture indépendante (docs/plans-de-side-v2.md §14). |
| D — onglet side refondu | Tuiles par carte, aperçu et écarts de candidats, bloc Extra, barre d'action mobile, annuler / vider, « Enregistrer et ouvrir », règle des 3 copies ; e2e `side` ; tag `side-v2-d-ok` | ✅ Terminée le 22 sept. 2026 — `SidePlanner` réécrit sur le store (adversaire ouvert = deck étudié, une tuile par carte, clic +1 / clic droit −1, écarts des candidats et aperçu du store, blocs Extra, barre d'action collante, annuler / vider, raisons visibles, « Enregistrer et ouvrir », panneau sous le plan sous 1024 px), règle des 3 copies toutes zones dans `addCard` / `setCopies`, le dialogue d'ajout et l'import (`overTotal`, réduction side puis extra) ; e2e `side` réécrit et `extraside` Z3 bis ; 377 web + 22 serveur, 12 scénarios e2e, 5 mutations détectées (D5 après ajout d'une garde), relecture indépendante (docs/plans-de-side-v2.md §15). |
| E — fiche, PDF, clôture | Extra dans la fiche et le PDF, e2e `sidesheet`, docs ; tags `side-v2-e-ok`, `side-v2-ok` | ✅ Terminée le 22 sept. 2026 — cadres de la fiche groupés par zone (`groupByZone`, une fois dans `sheetOf` : main, « Extra », « Zone inconnue »), PDF avec intertitre et hauteurs cohérentes, comparateur et serveur inchangés mais prouvés sur un plan à Extra (tests, e2e `sidesheet` F2–F5, intégration 10B) ; 383 web + 22 serveur, 13 + 17 + 13 PostgreSQL, 12 scénarios e2e, 5 mutations détectées, relecture indépendante (aucun défaut bloquant ; remarques 3, c1, c2, c3, c5 traitées) (docs/plans-de-side-v2.md §16) ; clôture §17, tags `side-v2-e-ok` et `side-v2-ok`. **Chantier clos** (A–E). |

## Reports connus (à reprendre dans l'étape indiquée)

- ~~Étape 9 (questions ouvertes de 7B)~~ : **traités en 9A** (docs/etape-9.md) — mur compact, Nouvelles mains 32 px, Δ 10 px, densité close à 96 px (mesure 84 px consignée), arbre ET/OU profond validé. Aucun test React dans le dépôt ; les gardes navigateur sont dans `web/e2e/` (hors `npm test`, Docker et Chrome requis) et mesurent le rendu de Chrome avec la police système de Windows.
- ~~Étape 9 (ancien report hors étape) : recalcul des aperçus de l'accueil~~ : **traité en 9A**.
- ~~Étape 9B (retours de la ressaisie) : vert intempestif, mode Non-engine + profil combiné~~ : **traités en 9B** (docs/etape-9.md, « Compte rendu 9B » ; valeur par défaut du déroulant = « profil inchangé »). ~~Étape 9C : extra et side éditables (avertissement au-delà de 15), runbook « déploiement courant » (base non vide, 003 journalisée, aucune question attendue), clôture~~ : **traités en 9C** (docs/etape-9.md, « Compte rendu 9C »). Questions ouvertes de 9C : Q14 (Inventaire en ambre au-delà de 15), Q15 (détail de carte sur les tuiles extra / side), Q16 (repli par zone), Q17 (`rehearsal.sh` sur une archive déjà à 003), Q18 (rétention de `keep/`).
- ~~Étape 9B (issu de 8C) : attente de la DB fondée sur `healthy` dans `deploy.sh`~~ : **traitée en 9B** — `db_ready` (lib.sh) exige `healthy` si healthcheck, puis le dernier « ready to accept connections » après le marqueur de l'entrypoint (« init process complete » ou « Skipping initialization »), puis `select 1` ; prouvée par le cas I de `test-migration-sequence.sh` (volume neuf, témoin du serveur temporaire) et `rehearsal.sh --fixture`. Questions ouvertes de 9B : Q9 (profil retiré avec la dernière étiquette même en « profil inchangé »), Q10 (vue héritée non rétablie à l'ouverture). Questions ouvertes de 8 conservées : Q11 (`ygo_previous`) ; Q12 / Q13 sans objet tant qu'aucun déploiement sur base conservée n'est prévu ; équivalence `--emit-sql` / `--apply` de prune-stale-cards non prouvée depuis 8A.
## Compte rendu de la dernière étape

À mettre à jour en fin de chaque tâche (remplacer le contenu, l'historique reste dans docs/etapes-*.md et git).

- **Date** : 22 septembre 2026.
- **Étape** : hors numérotation, refonte des plans de side, partie E (docs/plans-de-side-v2.md §16) et clôture (§17). Tags `side-v2-e-ok` et `side-v2-ok`, rien poussé. **Chantier clos** (A–E, le même jour, §12 à §16).
- **Livré** : l'Extra Deck dans la fiche et le PDF (cadres groupés par zone : main, « Extra », « Zone inconnue » ; `SheetPlan.groups` calculé une fois, lu par l'écran et le PDF ; `boxHeight` réserve l'intertitre) ; comparateur et serveur inchangés, prouvés sur un plan à Extra (main dérivé identique, plan d'Extra seul = deck de base, contrôle de taille toutes zones juste pour un plan prêt) ; e2e `sidesheet` étendu (carte synthétique « Extra Chi », F2 intertitre, F3 S7 à l'écran, F4 « EXTRA » dans le PDF, F5 comparateur sur le plan premier avec Extra) ; test d'intégration 10B avec un échange d'Extra ; docs, charte, AGENTS, DECISIONS ; clôture.
- **Vérifications exécutées** : typecheck, build, test-quiet (383 web, 22 serveur), intégration PostgreSQL 13 + 17 + 13 sur 55433 (conteneur détruit), e2e complet (12 scénarios) sur le code final, 5 mutations détectées, relecture indépendante (aucun défaut bloquant ; remarques 3, c1, c2, c3, c5 traitées). Séquence et répétition non rejouées : aucune migration, rien de deploy/ ni du schéma touché (déploiement en variante courante).
- **Non fait / reporté** : points ouverts hors chantier (§17) — Q13 performance des gros decks à paires, avertissement « hors format » hors de l'onglet side, carte en main ET en extra (marginal), plan rendu incomplet par ✕ à refaire par un échange, deux `StatsPanel` montées sous 1024 px (préexistant).
- **Prochaine action** : déploiement de la refonte sur demande explicite (variante « déploiement courant », C0–C6 ; `__ENGINE_VERSION__` a changé : aperçus et chiffres de plan recalculés) ; puis chantier « Mains types » (docs/prompt-mains-types.md), greffé sur `lib/studiedDeck.ts` (D16).

Compte rendu précédent (partie D, même jour) :

- **Date** : 22 septembre 2026.
- **Étape** : hors numérotation, refonte des plans de side, partie D (docs/plans-de-side-v2.md §15). Tag `side-v2-d-ok`, rien poussé. Parties A à C le même jour (§12 à §14).
- **Livré** : onglet « Plans de side » réécrit sur le store (l'adversaire ouvert est le deck étudié, position de la barre ; une tuile par carte avec clic +1 / clic droit −1, écarts des candidats I1 / I2 / I3 et aperçu avant « Échanger » ; blocs Extra → à sortir et Side → vers l'extra ; barre d'action collante ; « Annuler l'échange », « Vider le plan », ✕ = toutes les copies ; raisons visibles ; « Enregistrer et ouvrir la fiche / comparer » ; panneau « Probabilités » sous le plan sous 1024 px) ; règle des 3 copies toutes zones (S9, Q1) dans `addCard` / `setCopies`, le dialogue d'ajout (« max ») et l'import (`overTotal`, réduction side puis extra, jamais main) ; e2e `side` réécrit (P1–P6), `extraside` Z3 bis ; corrections de la relecture.
- **Vérifications exécutées** : typecheck, build, test-quiet (377 web, 22 serveur), e2e complet (12 scénarios) sur le code final, 5 mutations détectées (D5 après ajout d'une garde), relecture indépendante (E1 bloquant corrigé pendant la relecture ; E2–E5, E7–E9, c1–c3, c6, c9 corrigés).
- **Non fait / reporté** : partie E (Extra dans la fiche et le PDF, comparateur sur un plan avec Extra, e2e `sidesheet`, clôture) ; intégration PostgreSQL à rejouer avant la clôture (aucune migration : séquence et répétition sans objet, à confirmer en E).
- **Prochaine action** : partie E selon docs/plans-de-side-v2.md §8.

Compte rendu précédent (partie C, même jour) :

- **Date** : 22 septembre 2026.
- **Étape** : hors numérotation, refonte des plans de side, partie C (docs/plans-de-side-v2.md §14). Tag `side-v2-c-ok`, rien poussé. Parties A et B le même jour (§12, §13, tags `side-v2-a-ok`, `side-v2-b-ok`).
- **Livré** : barre de contexte (`StudyBar`, une seule bascule premier / second, sélecteur du deck étudié, raison d'un plan sans chiffre) ; `columnOf` / `studiedSource` ; panneau, matrice, mode Requête, mur de mains et tuiles sur le deck étudié, chacun nommé (S1), rien sans plan prêt (S2), aperçu dans la colonne de la position ouverte (S3), « … » tant que les écarts du deck sidé manquent ; badges « sort / entre » et bandeau de l'onglet Annoter (D14) ; e2e `study` ; corrections de la relecture.
- **Vérifications exécutées** : typecheck, build, test-quiet (376 web, 22 serveur), e2e complet (12 scénarios) sur le code final, 5 mutations détectées, relecture indépendante.
- **Non fait / reporté** : parties D (onglet refondu : aperçu et candidats affichés, panneau sous le contenu au téléphone, règle des 3 copies) et E (fiche, PDF, clôture) ; intégration PostgreSQL, séquence et répétition à rejouer avant la clôture.
- **Prochaine action** : partie D selon docs/plans-de-side-v2.md §7.

Compte rendu précédent (partie B, même jour) :

- **Date** : 22 septembre 2026.
- **Étape** : hors numérotation, refonte des plans de side, partie B (docs/plans-de-side-v2.md §13). Tag `side-v2-b-ok`, rien poussé. Partie A le même jour (§12, tag `side-v2-a-ok`).
- **Livré** : mode `second` du worker ; `store/study.ts` (decks étudiés par entrée du moteur, réutilisation du deck de base pour un plan vide ou un échange neutre, passes puis résultat complet derrière le deck de base, cache, aperçu sur client dédié, candidats sur un troisième client avec budget Q5, persistance des chiffres de plan) ; état et actions du store ; contexte d'étude dans l'URL (`lib/studyUrl.ts`, `EditorPage`) ; corrections de la relecture (annotation de side → resynchronisation, `lastPassMs`, annulation en ordre inverse, résultat périmé du seul même adversaire).
- **Vérifications exécutées** : typecheck, build, test-quiet (373 web, 22 serveur), e2e 11 scénarios (rejoué après les corrections), 10 mutations (9 détectées, 1 équivalente), relecture indépendante.
- **Non fait / reporté** : parties C à E ; `SidePlanner` garde son propre client jusqu'à D ; intégration PostgreSQL, séquence et répétition non rejouées (rien de ce qu'elles couvrent n'a changé), à rejouer avant la clôture.
- **Prochaine action** : partie C (éditeur en contexte) selon docs/plans-de-side-v2.md §7.

Compte rendu précédent (annotations par défaut, partie D) :

- **Date** : 17 septembre 2026.
- **Étape** : hors numérotation, annotations par défaut, partie D (docs/annotations-par-defaut.md §16). Tag `annotations-d-ok`, rien poussé. **Chantier clos** (A–D).
- **Livré** : bandeau par compte (`user_notices` dans 006, `/api/auth/me` → `notices`, fermeture), mode Non-engine unique, badges d'origine, retour au défaut, formulaire de référence et page `/references`, scénario e2e `defaults`, contrat §9.
- **Vérifications exécutées** : typecheck, build, test-quiet (309 web, 22 serveur), `test:integration` (13 + 17 + 13), `test-migration-sequence.sh` (133 gardes), e2e (11 scénarios), `rehearsal.sh --fixture` conforme, 9 mutations détectées.
- **Non fait / reporté** : déploiement (runbook « déploiement courant » avec migration 006, recrée le conteneur `db`) ; attribution du premier référent (`backoffice-role.sh grant <email> --role referent`) ; `referencesVersion` lue par aucun client ; tests unitaires de `DefaultsNotice`.
- **Prochaine action** (utilisateur) : relire les comptes rendus §15–§16, pousser la branche et les tags, puis décider du déploiement.

Compte rendu précédent (partie C, même jour) :

- **Date** : 17 septembre 2026.
- **Étape** : hors numérotation, annotations par défaut, partie C (docs/annotations-par-defaut.md §15). Tag `annotations-c-ok`, rien poussé.
- **Livré** : migration 006 étendue et sa séquence (lot C1), contrat et routes (choix par aspect, références, rôle référent), `effectiveLibrary.ts` et tous ses appelants, archive JSON, `prune-stale-cards`, `annotations-report.ts --gap` ; défauts du lot C2 / C3 interrompu corrigés, puis ceux de la relecture indépendante.
- **Vérifications exécutées** : typecheck, build, test-quiet (301 web, 22 serveur), `test:integration` (12 + 17 + 13), `test-migration-sequence.sh` (130 gardes), `rehearsal.sh --fixture` conforme, e2e (10 scénarios), 10 mutations détectées.
- **Non fait / reporté** : partie D (interface) ; `referencesVersion` lue par aucun client ; ✕ du plafond fourni de base (D).
- **Prochaine action** : réponses aux questions de la partie D, puis D.

Compte rendu précédent (parties A et B, conservé pour la lecture de la passation) :

- **Date** : 16 septembre 2026.
- **Étape** : hors numérotation, annotations par défaut, parties A et B (docs/annotations-par-defaut.md §12 et §14). Tags `annotations-a-ok` et `annotations-b-ok`, rien poussé.
- **Livré** : A — `server/src/domain/cardDefaults.ts` (détection HOPT par fenêtre de limite nommée, profils par 4 gabarits), fixtures de textes réels, 7 tests, `scripts/annotations-report.ts` ; B — profil `reactive` (contrat, moteur, oracle, libellés), porte profil / étiquette levée (cas Q1 réécrit sur décision, gardes serveur / store / grille), migration `006-annotation-defaults.sql` (contrainte CHECK) branchée partout, contrat §3 révisé.
- **Vérifications exécutées** : typecheck, build, test-quiet (291 web, 21 serveur), `test:integration` (12 + 14 + 11), e2e (10 scénarios), `test-migration-sequence.sh` (99 gardes), `rehearsal.sh --fixture` conforme ; 6 + 5 mutations détectées ; relectures indépendantes par sous-agent (A et B). 006 appliquée sur la base de dev par stdin.
- **Non fait / reporté** : parties C (modèle, persistance, référent, 006 étendue) et D (interface) ; rapport d'écart par deck (attend la bibliothèque effective de C) ; renumérotation du fichier local `06-backoffice-login`.
- **Décisions ajoutées** : DECISIONS.md, section « Annotations par défaut » (parties A et B) ; résumé dans docs/decisions-compressees.md.
- **Prochaine action** : partie C selon docs/annotations-par-defaut.md §13 (lots C1–C3).

Compte rendu précédent (back-office, même jour, conservé pour la lecture de la passation) :

- **Date** : 16 septembre 2026.
- **Étape** : hors numérotation, back-office (socle en lecture seule), parties A et B (docs/backoffice.md §10). Tags `backoffice-a-ok` et `backoffice-ok`, rien poussé.
- **Livré** : migration 005 (`users.role`, tables `backoffice_*`, rôle PostgreSQL `testhand_backoffice` à privilèges par colonne, journal en ajout seul par déclencheur), `deploy/backoffice-role.sh`, séquence de `lib.sh` et `check-migration.sql` étendus, site `admin/` (.NET 10, Razor Pages : connexion scrypt compatible Node, TOTP obligatoire, session 8 h absolues, tableau de bord, comptes, fiche, journal, charte clair / sombre, en-têtes, bandeau d'environnement), image et service Compose, runbook B0–B7, README §10, docs/design-backoffice.md.
- **Vérifications exécutées** : `dotnet test` 168 / 168 (150 unitaires, 18 intégration sur 55442) ; `node admin/e2e/run.mjs` 49 / 49 ; `test-migration-sequence.sh` 86 gardes A–J ; typecheck, build, test-quiet (254 web, 14 serveur), `test:integration` (12 + 14 + 11), e2e web (10 scénarios), `rehearsal.sh --fixture` conforme ; 11 mutations détectées (docs/backoffice.md §10).
- **Non fait / reporté** : aucune action sur les comptes (lot suivant) ; DNS, Caddy et déploiement à faire par l'utilisateur (runbook B0–B7) ; questions ouvertes Q1–Q3 de docs/backoffice.md.
- **Décisions ajoutées** : DECISIONS.md, section « Back-office — hors numérotation » ; résumé dans docs/decisions-compressees.md.
- **Prochaine action** (utilisateur) : relire docs/backoffice.md, pousser la branche `feat/backoffice` et ses tags, puis dérouler la variante B0–B7 du runbook.

Compte rendu précédent (refonte visuelle, même jour, conservé pour la lecture de la passation) :

- **Date** : 16 septembre 2026.
- **Étape** : refonte visuelle — application du verdict de l'audit 01 (`docs/audit/01-visuel.md` §0 et §8, direction A « sombre relevé ») et navigation de l'éditeur ; compte rendu `docs/refonte-visuelle.md`. Purement visuel : moteur et `__ENGINE_VERSION__` intacts.
- **Livré** : variables CSS de rôle (`index.css`), quatre rôles de texte `text-fg-1..4` et accents `text-pos/warn/info/neg`, surfaces `ink-950..500` relevées, échelle `fontSize` fermée (corps 13 px, plancher 11 px, dérogation `text-cell` 10 px pour les cellules compactes du comparateur) ; en-tête d'une ligne, onglets soulignés dès 640 px et barre d'onglets en bas en dessous, contexte premier / second dans le panneau « Probabilités », `h-[100dvh]` ; cartes de chaleur opaques à texte choisi par seuil exact (`lib/colors.ts`) ; badges de tuile sous le nom des cartes ; cibles à 24 / 32 px ; jargon « §x » et « E[·] » retirés ; `lang="fr"`, `theme-color`, favicon ; garde `lib/tokens.test.ts` ; charte, AGENTS.md, DECISIONS.md, décisions compressées.
- **Vérifications exécutées** : `npm run typecheck`, `npm run build`, `node scripts/test-quiet.mjs` (283 web dont 29 nouveaux, 14 serveur), `npm run e2e -w web` (10 scénarios, port web 5178), re-mesure de l'audit en trois passes sur pile jetable et cartes réelles : échecs AA hors états désactivés / périmés 255 / 328 / 531 → **0 / 0 / 0** (360 / 768 / 1440), texte < 11 px 41 % → 0 (hors dérogation), en-tête à 360 px 118 → 49 px, premier contenu 266 → 130 px, onglets masqués 208 → 0 px ; 7 mutations détectées (dont une qui a révélé un défaut de la garde des tokens, corrigé).
- **Non fait / reporté** : Q1–Q6 de `docs/refonte-visuelle.md` §7 (nom du compte rendu, contexte sous 1024 px, icônes de la barre du bas, horodatage à 360 px, barre de modes sur deux lignes, assertion explicite de la barre du bas) ; variante claire, `:focus-visible`, `role="dialog"` / Échap, page de connexion ; rendu sur appareils réels non vérifié. Aucune commande vers le VPS ni la base de dev.
- **Décisions ajoutées** : DECISIONS.md, section « Refonte visuelle — application du verdict de l'audit 01 » ; résumé dans docs/decisions-compressees.md.
- **Prochaine action** (utilisateur) : relire les captures de `docs/refonte-visuelle/captures/`, répondre à Q1–Q6, puis décider du déploiement (runbook C0–C6, code seul).

## Compte rendu final de la mission (9 septembre 2026)

Neuf étapes livrées du 7 au 9 septembre 2026, chacune close par typecheck, build, tests, contrôle par mutation, docs et tag `etape-N-ok` (jamais poussé par l'agent) : (1) contrat métier, cas de référence P/N/S/C/M et oracle indépendant ; (2) calculs défectueux corrigés par tests de régression ; (3) configuration v2 par deck, transaction unique, révision, import / export ; (4) recalcul versionné, annulation, état périmé explicite ; (5) contexte unique premier / second, profils, plafonds partagés, conditions ET/OU, migration 002 ; (6) création / import équivalents, validation navigateur et mobile ; (7) comparateur et Excel à données identiques, gardes visuelles versionnées ; (8) migrations 001–003, purge exacte simulée puis acceptée par empreinte, sauvegarde vérifiée, restauration, répétition, déploiement 8C « départ à vide » sur le VPS ; (9) aperçus sans cache faisant autorité, finitions de 7B, attente de la DB corrigée, vue transitoire, mode Non-engine combiné, extra / side éditables, runbook « déploiement courant ». État final : 206 tests web, 8 serveur, 12 + 10 PostgreSQL, 8 scénarios e2e, 61 gardes A–I, répétition conforme ; moteur et oracles jamais modifiés pour faire passer un test ; valeurs de contrôle historiques (33,76 %, 3,54 %, 30,21 %) et identifiants des cas de référence intacts. Production : `9d281cf` sur base v2 purgée ; l'étape 9 se déploie par C0–C6. Reports hors mission : Q9–Q18, Q11 (`ygo_previous`), équivalence `--emit-sql` / `--apply`, healthcheck Compose (Q6), réexamen catégorie / profil.

Compte rendu précédent (9B, conservé pour la lecture de la passation) :

- **Date** : 9 septembre 2026.
- **Étape** : 9, partie B — point 3 (attente de la DB), vert intempestif du bouton Enregistrer, mode Non-engine + profil combiné ; compte rendu, écart au plan, mutations, questions ouvertes Q9 / Q10 et passation dans docs/etape-9.md (« Compte rendu 9B »). Tag `etape-9b-ok`, rien poussé.
- **Livré** : `deploy/lib.sh` (`db_ready` = `healthy` si healthcheck, puis dernier « ready to accept connections » après le marqueur de l'entrypoint « PostgreSQL init process complete » / « Skipping initialization » dans les journaux depuis `StartedAt`, puis `select 1` ; `db_container_id`, `db_startup_logs`, `db_definitive_server_announced`) ; `deploy/deploy.sh` (attente par `db_ready || die`) ; cas I de `test-migration-sequence.sh` (volume neuf par `docker create` + `docker cp` avec `98-slow.sql`, témoin du serveur temporaire, séquence lancée aussitôt, 12 gardes) ; vue du panneau transitoire (`statsView` hors d'`EditableDeck` et des params émis, acceptée en lecture, `setStatsView` sans `markDirty`) ; `lib/nonEngine.ts` (règle unique poser / retirer), `applyNonEngine` (deux requêtes au plus dans la file, étiquette d'abord, profil orphelin retiré), `ModeBar` (déroulant étiquette + profil radio, « profil inchangé » par défaut, chip du couple), badge « poser / retirer » sur la tuile et bandeau au survol ; tests `statsView.test.ts` (3), `nonEngine.test.ts` (2), `nonengine.test.ts` (7) ; scénario e2e `nonengine` (API vérifiée à chaque clic, fixture restaurée) ; AGENTS.md, deploy/README.md, runbook V4, DECISIONS.md, decisions-compressees.md.
- **Vérifications exécutées** : `npm run typecheck`, `npm run build`, `node scripts/test-quiet.mjs` (200 web, 8 serveur), `npm run test:integration -w server` (persistence 12, purge 10), `bash deploy/test-migration-sequence.sh` (A–I, 61 gardes), `npm run e2e -w web` (7 scénarios), `bash deploy/rehearsal.sh --fixture` ; 4 mutations détectées (db_ready à un seul select, vue qui salit, profil avant étiquette, badge retiré) ; conteneurs jetables supprimés. Un échec non reproduit de l'e2e `setup` sous charge concurrente (test A–I simultané) est consigné.
- **Non fait / reporté** : 9C (extra / side, runbook « déploiement courant », clôture). Aucun test existant modifié ; moteur, oracles, migrations intacts ; aucune commande vers le VPS ni la base de dev. Écart au plan consigné : un marqueur d'entrypoint est exigé dans tous les cas (la règle « quand cette ligne existe » acceptait la fenêtre du serveur temporaire).
- **Décisions ajoutées** : DECISIONS.md, section « étape 9, partie B » ; résumé dans docs/decisions-compressees.md.
- **Prochaine action** : 9C (docs/etape-9.md, « Passation vers 9C ») ; pousser d'abord les commits et les tags `etape-9a-ok`, `etape-9b-ok` ; la correction de `deploy.sh` se déploie ensuite par le runbook (base conservée).
