# Plan de la mission de fiabilisation — 9 étapes

Source du plan : docs/cas-reference.md, « Acceptation par étape du plan révisé ».
Cible : docs/regles-metier.md (contrat métier). Comptes rendus détaillés : docs/etapes-*.md.
Chaque session lit ce fichier au début et met à jour le statut et le compte rendu en fin de tâche.

## Étapes

| # | Étape | Résultat vérifiable attendu | Statut |
| --- | --- | --- | --- |
| 1 | Règles et exemples | Contrat traçable ; fractions de contrôle ; oracle distinct ; vérifications existantes maintenues | ✅ Terminée le 7 sept. 2026 — contrat métier, cas de référence P/N/S/C/M, oracle indépendant (`web/src/engine/reference/`), 26 tests ; aucun code applicatif modifié. |
| 2 | Calculs confirmés défectueux | Tests de régression reliés au moteur et à l'affichage ; masses, arrondis et conditions corrects | ✅ Terminée le 7 sept. 2026 — delta reconstruit après remplacement, colonne 5+ agrégée, passe impossible explicitement indisponible, note /10 à arrondi exact, binomiale bigint ; 6 tests (`corrections.test.ts`). |
| 3 | Modèle et persistance | Paires manuelles propres au deck, non-engine commun au compte, enregistrement cohérent, import/export et duplication testés | ✅ Terminée le 7 sept. 2026 — configuration v2 (paires par deck, conditions, disponibilités, révision, transaction unique), migration additive `001-deck-configuration.sql`, JSON v2, duplication ; tests web/serveur/PostgreSQL, migration jouée en base jetable seulement. |
| 4 | Recalcul | État périmé visible, actions possibles, invalidation immédiate, annulation et absence de réponse ancienne adoptée | ✅ Terminée le 7 sept. 2026 — client de calcul pur avec propriété des tâches et annulation (`worker/computeClient.ts`), versionnement `modelVersion`/`resultVersion`, invalidation dès la mutation, état périmé atténué avec son contexte, erreur + relance, ouvertures numérotées pour `loadDeck`/`saveDeck` ; 16 tests avec faux worker et horloge simulée (`recompute.test.ts`, `computeClient.test.ts`). |
| 5 | Chronologie et conditions | Comparaison exacte à l'oracle sur 5 + pioche ; mêmes règles dans tous les consommateurs ; ET/OU testés | ✅ Terminée le 7 sept. 2026 — partie A (moteur : contexte unique, sixième identifiée, profils, plafonds partagés, ET/OU ; oracle étendu + oracle Monte Carlo) et partie B (migration additive `002-profiles-and-conditions.sql`, profils/plafonds au compte, conditions ET/OU par deck, invalidation des résumés à toute modification globale, réglage unique premier/second, sixième carte distinguée, « départs théoriques », Excel et comparateur relibellés) ; 150 tests web, 6 serveur, 10 PostgreSQL. |
| 6 | Configuration, création et mobile | Même deck métier depuis création/import ; erreurs explicites ; actions et sélections utilisables aux largeurs retenues | ✅ Terminée le 7 sept. 2026 (tags `etape-6a-ok`, `etape-6-ok`) — partie A : parseur YDK et collage qui rapportent sans réduire, aperçu d'import avec décision explicite, constructeur strict, messages serveur rendus tels quels, ids catalogue stricts, équivalence création / YDK / JSON v2 ; partie B : validation au navigateur (étape 4, contexte, sixième carte, mode Profil, menu ⋯, ET/OU, avertissement sans profil, aperçu d'import) avec captures sur infrastructure jetable, defaut serveur du plafond partagé corrigé (CHECK avant ON CONFLICT), Q3 tranchée (delta de tuile atténué), mobile 360 / 390 / 768 corrigé (retours à la ligne, cibles 32 / 24 px) et largeurs inscrites au contrat §6 ; 164 tests web, 7 serveur, 11 PostgreSQL. |
| 7 | Comparateur et exports | Matrices côte à côte sur mobile, deltas non arrondis, données identiques entre analyse/Excel/comparateur | ✅ Terminée le 8 sept. 2026 (tags `etape-7a-ok`, `etape-7-ok`) — partie A : identité des données prouvée (`dataIdentity.test.ts` : matrice du panneau = comparateur = cellules Excel en égalité stricte, agrégats et formules Excel recalculées, carte sans profil et plafond partagé), « · » réservé au zéro exact et couleur par signe exact (D1, D7 ; formateurs partagés), gardes visuelles versionnées dans `web/e2e/` (`npm run e2e -w web`, pile jetable, M1–M6, fixture Deck A / Deck B, comparateur avec export réel) ; partie B : scénario `mobile` (verdict par point et par largeur à 360 / 390 / 768 / 1440, 24 captures, export réel), Q3 tranchée (A et B côte à côte sous 640 px en grille de deux colonnes à cellules compactes de 9 px et 20 px minimum par colonne, Δ en dessous en pleine largeur, lisibilité prouvée sur capture à 360, aucun repli à défilement), Q4 appliquée (32 px sur Exporter Excel, Comparer, ✕, ⇄ Inverser), en-tête du comparateur en `flex-wrap`, ligne de main du mur qui passe à la ligne au lieu d'écraser les cartes, gardes correspondantes (7 mutations détectées), contrat §5 / §6 et charte §6.3 mis à jour ; 177 tests web, 7 serveur, 11 PostgreSQL. |
| 8 | Déploiement | Scripts dans /deploy, inventaire et simulation, purge exacte annoncée, contrôles après migration et restauration testée | ✅ Terminée le 8 sept. 2026 (tags `etape-8a-ok`, `etape-8b-ok`, `etape-8c-ready`, `etape-8-ok`) — A : inventaire prouvé du modèle historique, migration `003-purge-legacy.sql` (refus nominatifs, rapport ligne par ligne, simulation annulée volontairement, acceptation par empreinte, purge exacte, contrôles, rejeu défensif), `schema.sql` à bloc historique conditionnel (D1), `prune-stale-cards` v2, suite `purge.integration.ts`, rapport de simulation sur le dump réel (185 paires, 15 exclusions, 15 prérequis convertis, 4 pertinences ; empreinte `e0efff5c…`). B : `deploy/lib.sh` (séquence de migration partagée par `deploy.sh` et `rehearsal.sh`, codes 0 / 1 / 2 / 3, marqueurs « SIMULATION TERMINÉE » / « PURGE APPLIQUÉE » exigés avec le code 0), `backup.sh` étendu sans changer le cron (`.sha256`, restauration dans `ygo_verify`, `.fingerprint`, `keep/` hors rétention), `restore.sh` réversible par bascule de bases (refus sans somme ou empreinte, sauvegarde de sécurité, `ygo_previous`, `--check-only`), `deploy.sh` (sauvegarde obligatoire, inventaire avant 001, simulation, « OUI » / `--expect` / `--accept`, contrôles après migration, app démarrée sans 003 si refus, arrêtée seulement sur contrôle KO), `rehearsal.sh` (répétition complète sur copie jetable : restauration, séquence, recalcul contre l'ancien moteur e890078 sur la base pré-migration, e2e sur la pile migrée, retour arrière), `scripts/recompute-check.ts`, `test-backup-restore.sh`, `test-migration-sequence.sh`, `docs/deploy-runbook.md` ; 001 et 002 ne se rejouent plus une fois 003 journalisée (rejeu prouvé). Répétition sur le dump réel conforme (empreinte `e0efff5c…` reproduite, 16 / 16 decks identiques à l'ancien moteur avec paires réinjectées, e2e conformes, retour arrière prouvé). Préparation de 8C le 8 sept. 2026 (tag `etape-8c-ready`) : décision « départ à vide » (rien n'est conservé, archive souvenir hors VPS par principe), variante V1–V7 du runbook, catalogue prouvé (`migrate-cards.js` depuis le conteneur `app`, 14 529 cartes en 13–16 s, `/api/health` = source), contrôle « base vide au départ » ajouté à `lib.sh` (cas G / H). C : exécutée sur le VPS le 8 sept. 2026 par l'utilisateur (variante « départ à vide », code `9d281cf`) — archive souvenir vérifiée et copiée hors VPS, volume recréé, 001–003 jouées à vide par `initdb`, `deploy.sh` sans question (code 0 au second lancement), catalogue chargé par `migrate-cards.js`, compte créé, contrôles V7 passés, app en service. Incident consigné (docs/etape-8.md « Compte rendu 8C », runbook V4) : au premier `deploy.sh` sur le volume neuf, « Attente de la DB » (`pg_isready` par le socket) a accepté le serveur temporaire d'initialisation de PostgreSQL ; l'empreinte initiale a échoué pendant son extinction (« the database system is shutting down »), code 1, rien d'appliqué ; second lancement conforme. `deploy.sh` non corrigé : report en étape 9. |
| 9 | Finitions interface | Réponses de 7B (docs/etape-7.md, « Réponses ») appliquées : (1) mur de mains sous 640 px compacté, récapitulatif « S 3 · U 1 » (+ note) à droite des cartes, cartes jamais déformées ; (2) « ↻ Nouvelles mains » promu en action primaire de 32 px, même règle qu'Enregistrer (6B) ; (3) densité de la grille d'annotation à 1440 px : tuile réduite si une disposition conserve trois cibles de 32 px, sinon 96 px et clore ; arbre ET/OU plus profond que « ET de clauses, OU de feuilles » validé au navigateur, corrigé seulement si cassé ; (4) Δ en cellules de 10 px sous 640 px (pleine largeur) ; plus le report hors étape : aperçus de l'accueil recalculés avec une version fiable, sans cache faisant autorité. Gardes `web/e2e/` correspondantes. | 🟨 En cours — plan validé le 9 septembre 2026 (docs/etape-9.md), découpage 9A / 9B / 9C. **9A terminée le 9 septembre 2026** (tag `etape-9a-ok`) : aperçus de l'accueil recalculés sans cache faisant autorité (version du moteur `__ENGINE_VERSION__` = hash des sources du calcul ; résumé joint à l'enregistrement seulement s'il est frais ; recalcul à la demande à l'accueil, passe premier seule, persisté par `PUT /decks/:id/summary` avec révision, 409 ignoré ; un résumé absent ou d'une autre version n'est jamais affiché) ; finitions 7B : mur compact sous 640 px (récapitulatif « S / U » + note à droite des cartes, ligne ≤ 80 px, huit mains par écran), « ↻ Nouvelles mains » à 32 px, Δ en cellules de 10 px à toute largeur, densité de la grille close à 96 px (84 px mesuré : delta coupé à 1440), arbre ET/OU profond validé au navigateur (seule correction : commandes de l'éditeur à 24 px) ; scénarios e2e `home` et `conditions`, gardes `mobile` / `guards` étendues ; 188 tests web, 8 serveur, 12 + 10 PostgreSQL ; 7 mutations détectées. Reste : 9B (attente de la DB, vert intempestif, mode Non-engine + profil combiné), 9C (extra / side, runbook « déploiement courant », clôture, tag `etape-9-ok`). |

## Reports connus (à reprendre dans l'étape indiquée)

- ~~Étape 9 (questions ouvertes de 7B)~~ : **traités en 9A** (docs/etape-9.md) — mur compact, Nouvelles mains 32 px, Δ 10 px, densité close à 96 px (mesure 84 px consignée), arbre ET/OU profond validé. Aucun test React dans le dépôt ; les gardes navigateur sont dans `web/e2e/` (hors `npm test`, Docker et Chrome requis) et mesurent le rendu de Chrome avec la police système de Windows.
- ~~Étape 9 (ancien report hors étape) : recalcul des aperçus de l'accueil~~ : **traité en 9A**.
- Étape 9B (retours de la ressaisie, plan validé dans docs/etape-9.md) : vert intempestif du bouton Enregistrer (vue du panneau transitoire), mode Non-engine + profil combiné (le bouton annonce l'effet du prochain clic ; valeur par défaut du déroulant à expliciter dans docs/etape-9.md). Étape 9C : extra et side éditables (avertissement au-delà de 15), runbook « déploiement courant » (base non vide, 003 journalisée, aucune question attendue), clôture.
- Étape 9B (issu de 8C, docs/etape-8.md « Compte rendu 8C ») : **attente de la DB fondée sur `healthy`** dans `deploy.sh` — l'attente actuelle (`until pg_isready -q -U ygo -d ygo`, socket Unix) accepte le serveur temporaire que l'entrypoint PostgreSQL lance pour initialiser un volume neuf ; l'empreinte initiale a échoué pendant son extinction (code 1, rien d'appliqué, second lancement conforme). Attendre l'état `healthy` du conteneur `db` ; comme la `healthcheck` de `docker-compose.prod.yml` utilise le même `pg_isready` par le socket, prouver que l'attente retenue exclut la fenêtre du serveur temporaire (TCP `127.0.0.1`, ou second « ready to accept connections »), sur un volume neuf en conteneur jetable, cas ajouté à `test-migration-sequence.sh` si la séquence est concernée ; `rehearsal.sh --fixture` rejouée. Le runbook (V4) porte la consigne manuelle en attendant. Questions ouvertes de 8 conservées : Q11 (`ygo_previous`) ; Q12 / Q13 sans objet tant qu'aucun déploiement sur base conservée n'est prévu ; équivalence `--emit-sql` / `--apply` de prune-stale-cards non prouvée depuis 8A.
## Compte rendu de la dernière étape

À mettre à jour en fin de chaque tâche (remplacer le contenu, l'historique reste dans docs/etapes-*.md et git).

- **Date** : 9 septembre 2026.
- **Étape** : 9, partie A — points 1 et 2 du plan validé (aperçus de l'accueil, finitions de 7B) ; plan complet, réponses, compte rendu, mesures, mutations et passation dans docs/etape-9.md. Tag `etape-9a-ok`, rien poussé.
- **Livré** : version du moteur `__ENGINE_VERSION__` (vite.config.ts, hash des sources du calcul) ; `web/src/lib/summary.ts` (résumé du résultat frais, `usableSummary` = forme + version) ; mode `first` du worker (passe premier seule, `second` explicitement indisponible) ; `saveDeck` joint le résumé frais ; accueil avec client de calcul propre, recalcul à la demande et persistance par `PUT /decks/:id/summary` (révision, 409 ignoré) ; serveur : `deckSummary.ts` (validation, taille = main enregistré), `GET /decks` avec résumé, `PUT /decks/:id` avec `summary` optionnel, endpoint `/summary` (ni révision ni `updated_at` modifiées) ; mur compact sous 640 px (cartes 60 px, « S / U » + note à droite, ligne ≤ 80 px), « ↻ Nouvelles mains » 32 px, Δ 10 px partout, grille close à 96 px (84 px mesuré et refusé : delta coupé), commandes de l'éditeur ET/OU à 24 px ; scénarios e2e `home` et `conditions`, gardes `mobile` (P3 Δ, P7 compact et 32 px) et `guards` (colonnes) étendues.
- **Vérifications exécutées** : `npm run typecheck`, `npm run build`, `node scripts/test-quiet.mjs` (188 web, 8 serveur), `npm run test:integration -w server` (persistence 12, purge 10), `npm run e2e -w web` (6 scénarios, P1–P8 conformes aux 4 largeurs), `bash deploy/test-migration-sequence.sh` (A–H, 49 gardes) ; 7 mutations détectées (docs/etape-9.md) ; conteneurs jetables supprimés.
- **Non fait / reporté** : 9B (attente de la DB `healthy`, vert intempestif, mode combiné), 9C (extra / side, runbook « déploiement courant », clôture). Seul test existant modifié : garde P7 « Nouvelles mains » 24 → 32 px (annoncée). Moteur, oracles, migrations, deploy/ intacts ; aucune commande vers le VPS ni la base de dev.
- **Décisions ajoutées** : DECISIONS.md, section « étape 9, partie A » ; résumé dans docs/decisions-compressees.md.
- **Prochaine action** : 9B (docs/etape-9.md, « Passation vers 9B ») ; pousser d'abord le commit et le tag `etape-9a-ok`.

Compte rendu précédent (8C, conservé pour la lecture de la passation) :

- **Date** : 8 septembre 2026 (8C exécutée sur le VPS), consigné le 9 septembre 2026.
- **Étape** : 8, partie C — exécution en production par l'utilisateur, variante « départ à vide » du runbook, code `9d281cf` (tag `etape-8c-ready`) ; compte rendu et incident dans docs/etape-8.md (« Compte rendu 8C »). **L'étape 8 est terminée** (tag `etape-8-ok`).
- **Réalisé sur le VPS** : V1–V3 archive souvenir vérifiée, copiée hors VPS, volume `pgdata` supprimé ; V4 base recréée, 001–003 jouées à vide par `initdb`, `deploy.sh` sans aucune question (code 0) ; V5 catalogue par `migrate-cards.js` depuis le conteneur `app` ; V6 compte ; V7 contrôles passés, app en service.
- **Incident** : au premier `deploy.sh` sur le volume neuf, « Attente de la DB » (`pg_isready` par le socket) a accepté le serveur temporaire d'initialisation de PostgreSQL ; l'empreinte initiale a échoué pendant son extinction (« the database system is shutting down »), code 1, rien d'appliqué ; second `deploy.sh` conforme. Les codes de retour et la garde « aucune question » ont tenu. `deploy.sh` **non corrigé** dans la session de clôture (aucun code) : consigne manuelle dans le runbook V4 (attendre `healthy`, pas une simple connexion), report « attente de la DB fondée sur `healthy` » en étape 9.
- **Livré (clôture, docs seulement)** : docs/etape-8.md (« Compte rendu 8C »), docs/deploy-runbook.md (état exécuté, V4 : attente de `healthy` et incident), docs/PLAN.md, DECISIONS.md, docs/decisions-compressees.md. Aucun script, test, migration ni fichier d'interface modifié ; aucune commande vers le VPS ni la base de dev par la session.
- **Vérifications exécutées** : aucune (documentation seule) ; les dernières preuves restent celles de la préparation de 8C (typecheck, build, 177 web / 7 serveur, test-migration-sequence A–H, rehearsal --fixture).
- **Non fait / reporté** : correction de `deploy.sh` (étape 9) ; Q11 ; Q12 / Q13 sans objet ; équivalence `--emit-sql` / `--apply`.
- **Décisions ajoutées** : DECISIONS.md, section « étape 8, partie C » ; résumé dans docs/decisions-compressees.md.
- **Prochaine action** : étape 9 (finitions d'interface tranchées en 7B, recalcul des aperçus, attente de la DB fondée sur `healthy`) ; pousser d'abord le commit et le tag `etape-8-ok` (rien n'est poussé par cette session).

Compte rendu précédent (8B, conservé pour la lecture de la passation) :

- **Étape** : 8, partie B — points 3, 4 et 5 du plan validé (sauvegarde et restauration vérifiées, répétition sur le dump réel avec recalcul, `deploy.sh`, runbook) ; compte rendu, rapport de répétition, preuves, mutations, questions ouvertes et passation dans docs/etape-8.md (« Compte rendu 8B »).
- **Livré** : `deploy/lib.sh` (accès base par Compose ou conteneur jetable, empreintes `fingerprint.sql`, bases de travail gardées, `verify_backup`, inventaire `inventory.sql`, `simulate_003` / `apply_003` à marqueurs, acceptation « OUI » / `auto` / empreinte, `check_after_migration` avec `check-migration.sql` et empreintes, `run_migration_sequence` à crochets d'app, codes 0 / 1 / 2 / 3) ; `backup.sh` étendu sans changer le cron (`.sha256`, restauration dans `ygo_verify`, `.fingerprint`, ligne « sauvegarde ok » intacte, « vérifiée » / « NON vérifiée » + code 1, `--pre-migration` et `--keep` dans `keep/` hors rétention) ; `restore.sh` (refus sans somme ou empreinte, contrôle dans `ygo_restore`, sauvegarde de sécurité, bascule vers `ygo_previous`, `--check-only`) ; `deploy.sh` (séquence partagée, `--expect`, `--accept`) ; `rehearsal.sh` (archive ou `--fixture`, séquence, recalcul, e2e sur pile migrée par `run.mjs --db`, retour arrière, rapport) ; `scripts/recompute-check.ts` (référence = ancien moteur e890078 matérialisé depuis git sur la base pré-migration, (a) paires réinjectées identique, (b) écart après purge chiffré, résumé stocké informatif, `--fabricate`) + `scripts/tsconfig.json` / `package.json` et typecheck racine ; `test-backup-restore.sh`, `test-migration-sequence.sh` ; `docs/deploy-runbook.md` ; `web/e2e/run.mjs --db --db-container`, `cards.mjs` comblant seulement les images manquantes ; AGENTS.md (exception retirée), server/AGENTS.md, architecture, deploy/README (§7, §9), configuration-v2, DECISIONS, decisions-compressees. Découvert et traité : 001 rejouée après 003 recréait `deck_requirements` (refus de 003) → 001 / 002 plus rejouées une fois 003 journalisée, migrations intouchées.
- **Vérifications exécutées** : `npm run typecheck`, `npm run build`, `node scripts/test-quiet.mjs` (177 tests web, 7 serveur, inchangés), `npm run test:integration -w server` (persistence 11, purge 10), `npm run e2e -w web`, `bash deploy/test-backup-restore.sh` (57 gardes), `bash deploy/test-migration-sequence.sh` (cas A–F, 34 gardes), `bash deploy/rehearsal.sh --fixture` et répétition complète sur le dump réel (conforme : empreinte `e0efff5c…` reproduite, 16 / 16 decks identiques à l'ancien moteur avec paires réinjectées, e2e conformes, retour arrière prouvé, 401 s) ; contrôle par mutation (voir docs/etape-8.md), fichiers restaurés et vérifiés par SHA-256. Conteneurs supprimés, ports libres.
- **Non fait / reporté** : exécution VPS (8C, runbook) ; ressaisie des 185 paires ; questions ouvertes Q11–Q13 ; équivalence `--emit-sql` / `--apply`. Aucun test existant modifié ; moteur, oracles, 001 / 002 / 003, prune-stale-cards et interface intacts ; aucune commande vers le VPS ni la base de dev ; dump réel hors dépôt.
- **Décisions ajoutées** : DECISIONS.md, section « Première mission — étape 8, partie B » ; résumé dans docs/decisions-compressees.md.
- **Prochaine action** : 8C par l'utilisateur avec docs/deploy-runbook.md (pousser le commit et le tag `etape-8b-ok` d'abord, jamais poussés par cette session) ; ensuite l'étape 9.
