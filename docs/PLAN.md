# Plan de la mission de fiabilisation — 9 étapes

Source du plan : docs/cas-reference.md, « Acceptation par étape du plan révisé ».
Cible : docs/regles-metier.md (contrat métier). Comptes rendus détaillés : docs/etapes-*.md.
Chaque session lit ce fichier au début et met à jour le statut et le compte rendu en fin de tâche.

## Étapes

| # | Étape | Résultat vérifiable attendu | Statut |
| --- | --- | --- | --- |
| 1 | Règles et exemples | Contrat traçable ; fractions de contrôle ; oracle distinct ; vérifications existantes maintenues | ✅ Terminée le 7 sept. 2026 — contrat métier, cas de référence P/N/S/C/M, oracle indépendant (`web/src/engine/reference/`), 26 tests ; aucun code applicatif modifié. |
| 2 | Calculs confirmés défectueux | Tests de régression reliés au moteur et à l'affichage ; masses, arrondis et conditions corrects | ✅ Terminée le 7 sept. 2026 — delta reconstruit après remplacement, colonne 5+ agrégée, passe impossible explicitement indisponible, note /10 à arrondi exact, binomiale bigint ; 6 tests (`corrections.test.ts`). |
| 3 | Modèle et persistance | Paires manuelles propres au deck, non-engine commun au compte, enregistrement cohérent, import/export et duplication testés | ✅ Terminée le 7 sept. 2026 — configuration v2 (paires par deck, conditions, disponibilités, révision, transaction unique), migration additive `001-deck-configuration.sql`, JSON v2, duplication ; tests web/serveur/PostgreSQL, migration jouée en base jetable seulement. |
| 4 | Recalcul | État périmé visible, actions possibles, invalidation immédiate, annulation et absence de réponse ancienne adoptée | ✅ Terminée le 7 sept. 2026 — client de calcul pur avec propriété des tâches et annulation (`worker/computeClient.ts`), versionnement `modelVersion`/`resultVersion`, invalidation dès la mutation, état périmé atténué avec son contexte, erreur + relance, ouvertures numérotées pour `loadDeck`/`saveDeck` ; 16 tests avec faux worker et horloge simulée (`recompute.test.ts`, `computeClient.test.ts`). |
| 5 | Chronologie et conditions | Comparaison exacte à l'oracle sur 5 + pioche ; mêmes règles dans tous les consommateurs ; ET/OU testés | ✅ Terminée le 7 sept. 2026 — partie A (moteur : contexte unique, sixième identifiée, profils, plafonds partagés, ET/OU ; oracle étendu + oracle Monte Carlo) et partie B (migration additive `002-profiles-and-conditions.sql`, profils/plafonds au compte, conditions ET/OU par deck, invalidation des résumés à toute modification globale, réglage unique premier/second, sixième carte distinguée, « départs théoriques », Excel et comparateur relibellés) ; 150 tests web, 6 serveur, 10 PostgreSQL. |
| 6 | Configuration, création et mobile | Même deck métier depuis création/import ; erreurs explicites ; actions et sélections utilisables aux largeurs retenues | ✅ Terminée le 7 sept. 2026 (tags `etape-6a-ok`, `etape-6-ok`) — partie A : parseur YDK et collage qui rapportent sans réduire, aperçu d'import avec décision explicite, constructeur strict, messages serveur rendus tels quels, ids catalogue stricts, équivalence création / YDK / JSON v2 ; partie B : validation au navigateur (étape 4, contexte, sixième carte, mode Profil, menu ⋯, ET/OU, avertissement sans profil, aperçu d'import) avec captures sur infrastructure jetable, defaut serveur du plafond partagé corrigé (CHECK avant ON CONFLICT), Q3 tranchée (delta de tuile atténué), mobile 360 / 390 / 768 corrigé (retours à la ligne, cibles 32 / 24 px) et largeurs inscrites au contrat §6 ; 164 tests web, 7 serveur, 11 PostgreSQL. |
| 7 | Comparateur et exports | Matrices côte à côte sur mobile, deltas non arrondis, données identiques entre analyse/Excel/comparateur | ✅ Terminée le 8 sept. 2026 (tags `etape-7a-ok`, `etape-7-ok`) — partie A : identité des données prouvée (`dataIdentity.test.ts` : matrice du panneau = comparateur = cellules Excel en égalité stricte, agrégats et formules Excel recalculées, carte sans profil et plafond partagé), « · » réservé au zéro exact et couleur par signe exact (D1, D7 ; formateurs partagés), gardes visuelles versionnées dans `web/e2e/` (`npm run e2e -w web`, pile jetable, M1–M6, fixture Deck A / Deck B, comparateur avec export réel) ; partie B : scénario `mobile` (verdict par point et par largeur à 360 / 390 / 768 / 1440, 24 captures, export réel), Q3 tranchée (A et B côte à côte sous 640 px en grille de deux colonnes à cellules compactes de 9 px et 20 px minimum par colonne, Δ en dessous en pleine largeur, lisibilité prouvée sur capture à 360, aucun repli à défilement), Q4 appliquée (32 px sur Exporter Excel, Comparer, ✕, ⇄ Inverser), en-tête du comparateur en `flex-wrap`, ligne de main du mur qui passe à la ligne au lieu d'écraser les cartes, gardes correspondantes (7 mutations détectées), contrat §5 / §6 et charte §6.3 mis à jour ; 177 tests web, 7 serveur, 11 PostgreSQL. |
| 8 | Déploiement | Scripts dans /deploy, inventaire et simulation, purge exacte annoncée, contrôles après migration et restauration testée | 🔶 Partie A terminée le 8 sept. 2026 (tag `etape-8a-ok`) — inventaire prouvé du modèle historique (catalogue jetable + grep), migration `003-purge-legacy.sql` (refus nominatifs sur donnée non convertie, rapport ligne par ligne, simulation par sous-transaction annulée volontairement avec message et code distincts, acceptation par empreinte du rapport, purge exacte, contrôles avant/après, rejeu défensif), `schema.sql` à bloc historique conditionnel (D1), `relevance` plus écrite, `prune-stale-cards` v2 (refus avant 003, paires par deck, feuilles JSON en jsonpath strict, conflits = annulation totale), suite `purge.integration.ts` (10 tests, faux Supabase local, fixture de production pré-001), rapport de simulation produit sur le dump réel du 8 sept. 2026 (185 paires, 15 exclusions, 15 prérequis convertis, 4 pertinences ; purge appliquée puis vérifiée sur la copie jetable) ; 6 mutations détectées. Partie B (sauvegarde et restauration vérifiées, répétition sur dump réel avec recalcul, `deploy.sh`, runbook) et partie C (exécution VPS) à venir ; `deploy.sh` ne rejoue pas encore 003. |
| 9 | Finitions interface | Réponses de 7B (docs/etape-7.md, « Réponses ») appliquées : (1) mur de mains sous 640 px compacté, récapitulatif « S 3 · U 1 » (+ note) à droite des cartes, cartes jamais déformées ; (2) « ↻ Nouvelles mains » promu en action primaire de 32 px, même règle qu'Enregistrer (6B) ; (3) densité de la grille d'annotation à 1440 px : tuile réduite si une disposition conserve trois cibles de 32 px, sinon 96 px et clore ; arbre ET/OU plus profond que « ET de clauses, OU de feuilles » validé au navigateur, corrigé seulement si cassé ; (4) Δ en cellules de 10 px sous 640 px (pleine largeur) ; plus le report hors étape : aperçus de l'accueil recalculés avec une version fiable, sans cache faisant autorité. Gardes `web/e2e/` correspondantes. | ⬜ Non commencée — après le déploiement (étape 8) ; rien n'en est implémenté à l'étape 8. |

## Reports connus (à reprendre dans l'étape indiquée)

- Étape 9 (questions ouvertes de 7B, docs/etape-7.md, tranchées dans sa section « Réponses » ; à implémenter en 9, rien à l'étape 8) : densité du mur de mains sous 640 px (récapitulatif à la ligne, 124 px par main, ou récapitulatif compact « S 3 · U 1 ») ; « ↻ Nouvelles mains » à 24 px, à promouvoir ou non en cible de 32 px ; matrice Δ sous 640 px en cellules compactes (9 px) alors qu'elle a la largeur pour 10 px ; reports 6B jamais traités : densité de la grille d'annotation sur bureau (9 colonnes à 1440 px depuis les cibles de 32 px, 11 avant) et mise en page d'un arbre ET/OU plus profond que « ET de clauses, OU de feuilles » (seul cas produit par l'interface). Aucun test React dans le dépôt ; les gardes navigateur sont dans `web/e2e/` (hors `npm test`, Docker et Chrome requis) et mesurent le rendu de Chrome avec la police système de Windows.
- Étape 8, partie B (docs/etape-8.md, « Passation vers 8B ») : `backup.sh` existant (cron) à étendre, pas à doubler (archive horodatée, `.sha256`, vérification par restauration dans `ygo_verify`, empreinte de données, `keep/` hors rétention) ; `restore.sh` réversible ; `rehearsal.sh` sur le dump réel avec recalcul comparé aux résumés d'avant purge (résumés du jeu représentatif à fabriquer par le moteur) ; `deploy.sh` (sauvegarde obligatoire, simulation, « OUI » + `--accept <empreinte>`, contrôles, app démarrée sans 003 si refus) et `docs/deploy-runbook.md` (copie de l'archive pré-migration hors VPS, empreinte vérifiée, avant le point de non-retour) ; mutation M2. Partie C : exécution VPS. Non prouvé en 8A : équivalence complète `--emit-sql` / `--apply` de prune-stale-cards.
- Étape 9 (ancien report hors étape) : recalcul des aperçus de l'accueil avec une version fiable, sans cache faisant autorité.

## Compte rendu de la dernière étape

À mettre à jour en fin de chaque tâche (remplacer le contenu, l'historique reste dans docs/etapes-*.md et git).

- **Date** : 8 septembre 2026.
- **Étape** : 8, partie A — points 1 et 2 du plan validé (inventaire, migration 003, prune-stale-cards v2, D1, harnais) ; compte rendu, inventaire, réponses, rapport du dump réel et passation dans docs/etape-8.md.
- **Livré** : `db/migrations/003-purge-legacy.sql` (refus nominatifs : 001/002 non journalisées, prérequis historique absent de deck_requirements, prérequis v2 sans condition, horizon résiduel ; rapport compte / P1–P6 / avant / résumé / empreinte / après / statut par `select` et `raise notice` ; mode `testhand.purge_mode = 'simulate'` = exécution réelle dans une sous-transaction annulée volontairement, code 0, ligne « SIMULATION TERMINÉE » ; `testhand.purge_accept = '<empreinte>'` exigée dès qu'une ligne est purgée ; effectifs des tables conservées comparés avant/après ; rejeu = refus de tout objet historique réapparu) ; `db/schema.sql` avec bloc « Modèle historique » conditionnel à « 003 non journalisée » et `relevance` à défaut `'both'` ; serveur sans écriture de `relevance` ; `prune-stale-cards.ts` v2 (refus avant 003 ; deck_combo_pairs avec survivante non concernée et paire dégénérée supprimée ; deck_conditions source et feuilles JSON en jsonpath strict ; deck_flags et card_flags fusionnés ; conflits = annulation nommée ; `--emit-sql` avec JSON inliné) ; `server/tests/purge.integration.ts` (10 tests) + fixture `legacy-representative.sql` ; montages Compose et harnais e2e avec 003 ; `.gitignore` (`*.sql.gz`, `deploy/out/`) ; AGENTS.md, server/AGENTS.md, architecture, deploy/README, configuration-v2 (section 003), DECISIONS, decisions-compressees. Rapport de simulation sur le dump réel du 8 sept. 2026 joint à docs/etape-8.md (185 paires globales, 15 exclusions, 0 prérequis source paire, 15 prérequis convertis, 4 pertinences, 0 drapeau ; empreinte `e0efff5c…`), purge appliquée puis vérifiée sur la copie jetable (conditions, cartes et starters identiques).
- **Vérifications exécutées** : `npm run typecheck`, `npm run build` (avertissement ExcelJS attendu), `node scripts/test-quiet.mjs` (177 tests web, 7 serveur, inchangés) ; `npm run test:integration -w server` sur conteneur jetable 55433 recréé (persistence 11, purge 10) ; `npm run e2e -w web` complet sur pile jetable migrée 003 (setup, guards, compare, mobile conformes) ; contrôle par mutation M1, M3a, M3b, M4, M5, M6, toutes détectées par la garde attendue, fichiers restaurés et vérifiés par empreinte SHA-256. Conteneurs 55433, 55435, 55437 et pile e2e supprimés.
- **Non fait / reporté** : points 3, 4 et 5 (8B), exécution VPS (8C) ; `deploy.sh` ne rejoue pas encore 003 ; mutation M2 ; équivalence `--emit-sql` / `--apply` ; résumés d'avant purge de la fixture. Aucun test existant modifié ; moteur, oracles, 001/002 et interface intacts ; aucune commande vers le VPS, le dump réel n'est ni commité ni copié dans le dépôt.
- **Décisions ajoutées** : DECISIONS.md, section « Première mission — étape 8, partie A » ; résumé dans docs/decisions-compressees.md.
- **Prochaine action** : 8B dans une nouvelle session : lire docs/etape-8.md (« Passation vers 8B »), deploy/backup.sh tel qu'il tourne en cron, deploy/README.md ; conteneurs jetables seulement ; le dump réel sert à la répétition.
